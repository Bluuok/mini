# 我的 MiniCode 学习清单（执行循环 / 工具契约 / 受控观察 / 写前确认 / 会话持久化）

> 我的背景：二本秋招 / 目标岗位 AI 应用开发 / 技术栈 TypeScript（不熟，需补课）/ 有 Java 基础但不体现 / 熟 Agent 基本流程，不熟 MCP。本项目=实习项目，口径以 `E:/zzzz/mini/MiniCode-定稿.md` 为唯一真相源。
>
> ⚠️ 选点已锁定（`MiniCode-项目定义-基准.md` §五）：**R01 执行循环 / R08 工具契约 / R09 受控观察 / R10 写前确认 / R18 会话持久化**。本清单 2026-08-30 已重写对齐；旧版（R02 状态机 / R15 Skills / R23 验证闭环）作废——这三块均未选，且在定稿禁区词表里，**面试严禁主动引出**。
>
> 资料在哪看：**网页端** `hhh/Agent学习资源库/Agent学习资源库-单文件版.html` → **「TinyCode 图解精读」（MiniCode 同源图解，共 27 章）**。本文档「定位」列=章节号+章节名，**已按 2026-08-30 当前 HTML 逐章核对**（旧版引用的「第 7 章代理循环」等章节名在当前网页端已不存在，以本清单为准）。
>
> 配套文件：口径真相源 `MiniCode-定稿.md`（bullet/禁区词表/护身答案/Q&A）；基准 `MiniCode-项目定义-基准.md`；三项目关系 `三项目联动-步6.md`。

---

## 一、简历写什么

| 简历点 | 角色 | 一句话 |
| --- | --- | --- |
| R01 | Agent 执行循环 | 多步骤文档任务闭环执行 + 终止保护（讲清即可） |
| R08 | 工具定义与注册契约 | Schema 生成端引导 + 运行时校验兜底（讲清，双份定义追问必背） |
| R09 | 受控文件观察 | 目录限制/分页/输出上限（**独立实现子系统，最扎实的一张牌**） |
| R10 | 写前确认 diff | 核心安全闭环（**主讲的 安全点**） |
| R18 | JSONL 会话持久化 | 可恢复、可回溯（讲清即可） |

**方向定位**：执行（R01）→ 契约（R08）→ 观察（R09）→ 确认（R10 主讲）→ 恢复（R18）。最敢往深讲的两张牌：R09（定稿与联动文档都标注「独立实现该子系统」）+ R10（基准文件定位的核心安全闭环）。

关键技术栈：TypeScript、Node.js、LLM Tool Calling、JSON Schema / Zod、JSONL 事件流、Agent Loop、diff 审查。

**与定稿的对应**：五点顺序=定稿 §一 bullet 顺序；学完每单元回头把定稿对应 bullet 与 Q&A 复述一遍，能脱稿讲四段式才算过关。

---

## 二、一条线：前置知识 → 点本身，逐点列清

> 每个点按「前置 → 主（点本身）→ QA」顺下来。前置是被追问时的兜底弹药，**只在被问到才展开**。

### 单元 0 · 全局骨架（先花半天，所有单元的地基）

- **主**：**第 1 章「它是什么」** + **第 6 章「三层三明治：引擎与规矩分离」**（表现层/策略层/内核层，依赖只准向下）+ **第 7 章「总装车间 bootstrap.ts」**（装配顺序）。
- 掌握门槛：能画「用户一句话 → 循环 → 工具执行 → 结果回填 → 再推理 → 写前确认 → 落盘 → 会话记录」的链路图；能说出三层各管什么。
- QA：无直接题，但定稿全部四段式都靠这张骨架撑。

### 单元 1 · R01 Agent 执行循环（讲清即可）

- **前置**：单元 0。
- **主**：**第 9 章「一句话的完整旅行」**（从回车到落盘一共八站，没干完带新结果回炉再走）。
- 掌握门槛：能讲循环主干（模型返回工具调用 → 注册表调度执行 → 结果回填 → 再推理 → 直到最终结果或最大步数终止）；能答 Q01.3「怎么防死循环」（maxSteps 按执行步数计，触顶带诊断终止）。
- **⚠️ 口径**：空响应重试/思考中断恢复属「异常状态机」——**未选点**，被追就答「我主要做循环编排+终止保护」（定稿 Q01 弱点追问）。
- 辅助：第 8 章「五根线，插进转圈圈」（规矩从五个口注入——被问「循环怎么装配」时用）。

### 单元 2 · R08 工具定义与注册契约（讲清，追问必背）

- **前置**：单元 1（工具怎么被循环调用）。
- **主**：**第 10 章「七件工具，三个工位」**（每件工具的行为契约与防失控上限）+ **第 11 章「一张桌子，四个来源」**（统一注册：不管工具来自哪，模型看到一张扁平菜单）。
- 掌握门槛：能讲 JSON Schema（给模型生成端引导）vs 运行时校验（执行端兜底）两层各自管什么；能答「两份定义怎么防漂移」=**定稿护身答案 2**（统一工具定义封装维护一致；单一事实源自动生成是已知改进方向——诚实交底必备）。
- QA：Q08.1 / Q08.2。

### 单元 3 · R09 受控文件观察（最扎实的独立点）

- **前置**：单元 2。
- **主**：**第 10 章**（read 单次 2000 行上限 / 单行 2000 字符截断 / grep 输出控制）。
- 辅助：**第 12 章「小抄骗不过双面镜」**（符号链接两侧都还原成真实磁盘位置再判定——「路径规范化」的深挖弹药）+ **第 16 章「门卫不是监狱：安全的边界」**（应用内检查非 OS 级隔离——支撑「不引沙箱」口径）。
- 掌握门槛：能答「这不是 RAG 吗」（Agent 主动文件查找+受控读取，无向量索引，读了什么全有记录=Q09.1）；能讲分页读取+按需续读（Q09.2）。
- 联动锚点：联动文档 Round 2 就靠这条扛「实习归属」题——细节必须能口头重述。

### 单元 4 · R10 写前确认 diff（主讲的 安全闭环）

- **前置**：**第 13 章「给每条命令判个级」**（bash 命令 safe/write/destructive 三级——理解「分级审批」思想）+ **第 14 章「排队规则：谁先发言谁说了算」**（静态规则→会话记忆→模式开关→问本人的短路裁决流水线）。
- **主**：**第 15 章「同一道闸门，两种前台」**（once/always/deny 三种人工答复；无头模式回调缺席=默认拒绝）。
- 掌握门槛：能讲完整流程（修改先算统一 diff → 预览 → 用户确认 → 才落盘；**确认由用户完成而非模型自行通过**）；能答 Q10.2「为什么 diff 优于确认按钮」（diff 展示增删行+上下文，给足判断材料）。
- **⚠️ 口径**：只讲「写文件这道确认闸」；**绝不展开成权限状态机 / PermissionManager 生命周期**（那是未选点 R12，定稿禁区词表明确）。「总是允许」说成「会话内记住选择」即可，别造权限生命周期故事。

### 单元 5 · R18 JSONL 会话持久化（讲清即可）

- **前置**：单元 1（事件在循环里怎么产生）。
- **主**：**第 19 章「只加页、不撕页的日记本」**（追加式 JSONL：一行一事件、崩溃安全、第一行永远是会话头）+ **第 20 章「新本子、旧本子、换本子」**（新建/续接会话、旧会话归档不撕毁）。
- 掌握门槛：能讲 append-only 为什么崩溃不易损坏（Q18.1）；能讲 fork=从某点创建独立探索分支、不污染原会话（Q18.2，**单用户重试分支**，不是多人协作）。
- **⚠️ 不夸大**：单机单用户的会话持久化，不是分布式存储/协作系统；文件最终版本以实际写入为准（Q&A 弱点追问口径）。

---

## 三、资料速查

**路线 A · 网页端（日常学习，推荐）**：打开 HTML → 「TinyCode 图解精读」。章节对照：

| 单元 | 前置 | 主 | 辅助 |
| --- | --- | --- | --- |
| 单元 0 | —（地基） | 第 1 / 6 / 7 章（它是什么 / 三层三明治 / 总装车间） | — |
| 单元 1（R01） | 单元 0 | 第 9 章（一句话的完整旅行） | 第 8 章（五根线） |
| 单元 2（R08） | 单元 1 | 第 10 / 11 章（七件工具 / 一张桌子四个来源） | — |
| 单元 3（R09） | 单元 2 | 第 10 章（防失控上限） | 第 12 / 16 章（双面镜 / 门卫不是监狱） |
| 单元 4（R10 主讲） | 第 13 / 14 章（命令分级 / 裁决流水线） | 第 15 章（同一道闸门，两种前台） | 第 16 章（安全边界口径） |
| 单元 5（R18） | 单元 1 | 第 19 / 20 章（只加页日记本 / 新本子旧本子） | — |

**路线 B · 源码回看（面试前必做）**：参考仓库本体在 `E:/zzzz/mini/minicode付费版内参/MiniCode-main/`。带读优先级（=定稿回看项 + 联动 Round 2 依赖）：① R09 受控观察三要素（目录限制/分页/输出上限）的实现位置 ② R10 diff 的计算方式与「diff → 确认 → 写入」路径 ③ R18 JSONL 逐事件的结构（消息/工具调用/结果/边界标记）④ R01 循环的终止检查位置 ⑤ R08 注册表调度入口。每项做到**能口头重述**即可撑住「参与实现」四个字。

---

## 四、注意事项

### 四条红线（违反即穿帮）

1. **禁区词表（定稿 §二）全局生效**：严禁引出 RAG / MCP / Skill / Context Compression / 多 Agent / Evaluation Framework / 权限状态机 / 沙箱。被问「接 MCP 了吗」→「工具自建注册，没接外部 MCP 生态」（Q08.2 局限口径）；被问语义检索 →「大规模+需语义召回才值得，目前受控查找够用」。
2. **不编造量化指标**：验证效果只用「受控 / 可追踪 / 可恢复 / 闭环执行」这类定性表述；实习业务数据一律从真实实习长出来，没有就标【待补充】。
3. **未实现不说已做**：单一事实源自动生成（R08 改进方向）、通用权限平台、异常状态机全套、fork 多人协作/合并。
4. **内部命名不进简历**：runAgentTurn / ToolDefinition 是我描述用的概念化命名，实际以团队代码为准（定稿护身答案 1）；简历正文已去内部名。

### 降级预案（讲不稳就换）

| 原要点 | 降级到 |
| --- | --- |
| R01 | 退「多轮闭环 + 最大步数终止」概念层，不展开回填细节 |
| R08 | 退「Schema 引导 + 运行时兜底」两层分工；漂移追问按护身答案 2 诚实交底 |
| R09 | 退「至少控制了单次读取规模」的上下文管理价值 |
| R10 | 退「当前只做写文件这道确认闸，没做成通用权限平台」 |
| R18 | 退「一行一事件、可恢复」，不展开 fork/归档细节 |

### 其他

- **编号铁律**：MiniCode 的 R10=写前确认 diff，Craft 的 R10=AgentEvent 统一事件流——**同号不同物**（联动铁律 3 的例子）；三套 Rxx 互不映射，面试只讲实质。
- **动词纪律**：R09=「实现了」（独立实现）；R01/R08/R10/R18=「参与实现 / 参与设计并实现」（定稿 bullet 已定死，口语严格对齐，永远带「方向与验收是团队共同定的」团队定语）。
- 每点过 30 秒电梯测试（对着定稿 §一 bullet 说）；回答一律四段式：解决问题 → 实现方式 → 设计取舍 → 局限与扩展。
- 网页端第 17/18 章（日志截断/上下文压缩）、第 21/22/23 章（技能/MCP/子代理）、第 25/26/27 章（模型注册表/测试）均属**未选点或禁区**——学习可浏览理解，面试一律不主动讲。
