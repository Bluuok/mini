# 
# 1HappyClaw 简历组合方案（付费版）

## 使用说明

每个方案只推荐 5 个点。真正写进简历时可以删到 4 个。不要把多个方案简单拼接，否则面试追问面会过大。

## 方案 A：基础保守版

### 推荐人群

项目经验较少、只想稳妥写一个高质量 Agent 工程项目的同学。

### 推荐组合

`R02 R13 R18 R21 R24`

### 项目定位

HappyClaw 是一个多用户 Agent 工作台项目，重点展示服务化架构、SQLite 数据层、Hono 路由体系、React 前端和工程化验证。

### 面试准备重点

`Q02.x Q13.x Q18.x Q21.x Q24.x`

## 方案 B：Agent Core 架构版

### 推荐人群

目标 AI Agent 架构、AI Infra、Agent 平台岗位，并能讲清楚 Agent 执行机制的同学。

### 推荐组合

`R01 R03 R05 R06 R15`

### 项目定位

HappyClaw 是一个围绕三层产品模型、双模式执行引擎、IPC 协议、StreamEvent 事件流和四段式 Prompt 工程构建的高阶 Agent 执行平台。

### 面试准备重点

`Q01.x Q03.x Q05.x Q06.x Q15.x`

### 风险提示

`R03 R05 R06` 是中高风险点。如果讲不清楚，`R03` 降级为 `R02`，`R05` 降级为 `R18`，`R06` 降级为 `R07`。

## 方案 C：多渠道 IM 平台版

### 推荐人群

想突出数字员工、IM 集成、消息路由、多渠道平台能力的同学。

### 推荐组合

`R07 R08 R09 R10 R11`

### 项目定位

HappyClaw 通过 7 渠道 IM 统一抽象、JID 寻址、出站可靠投递、Turn 租赁和多模型负载均衡，实现数字员工驻场企业通讯平台。

### 面试准备重点

`Q07.x Q08.x Q09.x Q10.x Q11.x`

### 风险提示

`R09 R10` 是中高风险点。如果讲不清楚，`R09` 降级为 `R08`，`R10` 降级为 `R07`。

## 方案 D：数据层与调度工程版

### 推荐人群

目标后端、平台工程、全栈架构岗位，并熟悉数据库迁移、任务调度、权限隔离的同学。

### 推荐组合

`R13 R14 R09 R20 R08`

### 项目定位

HappyClaw 通过 SQLite 三层迁移、V2 Materialization 调度器、出站投递状态机、RBAC 三态权限和 JID 路由，支撑多用户 Agent 平台的数据与调度基础设施。

### 面试准备重点

`Q13.x Q14.x Q09.x Q20.x Q08.x`

### 风险提示

`R14 R09 R20` 是中高风险点。如果讲不清楚，`R14` 降级为 `R13`，`R09` 降级为 `R08`，`R20` 降级为 `R19`。

## 方案 E：前端与实时体验版

### 推荐人群

目标前端、全栈、客户端工程，熟悉 React 和实时交互的同学。

### 推荐组合

`R21 R22 R06 R18 R17`

### 项目定位

HappyClaw 是一个基于 React 19 前端架构、实时流式输出、StreamEvent 事件流、Hono 路由和对话式 Agent Builder 的多端 Agent 体验平台。

### 面试准备重点

`Q21.x Q22.x Q06.x Q18.x Q17.x`

### 风险提示

`R06 R17` 是中高风险点。如果讲不清楚，`R06` 降级为 `R07`，`R17` 降级为 `R15`。

## 方案 F：Agent 能力治理版

### 推荐人群

想突出 Agent 工具调用、MCP、Skills、Plugins、能力治理和 Agent 生命周期的同学。

### 推荐组合

`R15 R16 R17 R01 R03`

### 项目定位

HappyClaw 通过四段式 Prompt、Skills/MCP/Plugins 分层治理、对话式 Agent Builder、三层模型和双模式执行，构建 Agent 能力治理与生命周期管理平台。

### 面试准备重点

`Q15.x Q16.x Q17.x Q01.x Q03.x`

### 风险提示

`R16` 是高风险点。如果讲不清楚，`R16` 降级为 `R15`，`R03` 降级为 `R02`。

## 方案 G：企业数字员工完整版

### 推荐人群

目标企业 AI 应用、数字员工、Agent 平台岗位，想展示完整数字员工能力的同学。

### 推荐组合

`R07 R14 R15 R20 R23`

### 项目定位

HappyClaw 是一个企业数字员工平台，通过 7 渠道 IM 驻场、定时任务自动化、四段式 Agent 人设、RBAC 多租户权限和安全隔离，实现可交付企业使用的数字员工产品。

### 面试准备重点

`Q07.x Q14.x Q15.x Q20.x Q23.x`

### 风险提示

`R23` 是高风险点。如果讲不清楚安全隔离的源码层设计，`R23` 降级为 `R19`。

## 方案 H：会员默认推荐版

### 推荐人群

不知道怎么选，想要一版最能体现高阶 Agent 平台、但不至于过度冒险的同学。

### 推荐组合

`R01 R07 R11 R15 R23`

### 项目定位

HappyClaw 是一个多用户 Agent 工作台 + 企业数字员工平台，围绕三层产品模型、7 渠道 IM 集成、多模型负载均衡、Agent Profile 工程和多租户安全隔离展开。

### 面试准备重点

`Q01.x Q07.x Q11.x Q15.x Q23.x`

### 风险提示

`R23` 是高风险点。如果讲不清楚多租户安全隔离，降级为 `R19`。

## 方案 I：前沿热点版（Agent 评测 / Skill 自进化 / Agent 安全）

### 推荐人群

目标高薪的 Agent 平台、AI Infra、Agent 安全岗位，面试官大概率追问评测、自进化、安全三个热点的同学。

### 推荐组合

`R25 R26 R27 R06 R15`

### 项目定位

HappyClaw 是一个把「评测、进化、安全」落到工程实处的多用户 Agent 平台：以 StreamEvent 全链路轨迹与回归门禁度量行为、以不可变快照与运行时门控治理能力、以沙箱/凭据/授权纵深防御守住信任边界。

### 面试准备重点

`Q25.x Q26.x Q27.x Q06.x Q15.x`

### 风险提示

`R26 R27` 是高风险点。三个前沿点必须区分【项目实现】与【前沿认知】——论文数字（SkillsBench +16.6pp、AI Control 15%→92%、26.1% 技能漏洞率）只作面试展开，不写进简历 bullet。讲不稳时：`R26` 降级 `R16`，再降 `R15`；`R27` 降级 `R23`，再降 `R19`。
