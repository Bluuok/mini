# 三项目简历 · 指挥手册 v2

> 用途：执行这个三项目简历计划的操作手册。每一步给模型发什么提示词、用哪个模型、何时切换、产出后检查什么。压缩后以本文件为准恢复进度。
> Skill 为准：`.claude/skills/paidgroup-agent-resume-skill/` → 各项目 refs 目录。

---

## 0. 三项目选点（已定稿，勿改）

| 项目 | 身份 | 场景标签 | 选点（5 个 R） |
| --- | --- | --- | --- |
| MiniCode | **实习项目** | 研发项目智能协作助手（文档协作，非Coding/RAG/办公平台） | R01 R08 R09 R10 R18 |
| Craft Agents | **个人项目** | 个人深度研究/信息分析工作台 | R03 R08 R10 R12 R18 |
| HappyClaw | **个人项目** | 跨境电商 AI 数字员工 | R07 R14 R15 R19 R20 |

> ⚠️ 编号核对（2026-08-29 同步：Craft R24→R10 换点 + HappyClaw R25→R19 补记）：
> - **craft R10** = AgentEvent 统一事件流与生命周期（事件契约/流式渲染/错误恢复）。**不是** MiniCode R10（写前确认 diff）——同号不同物。
> - **craft R18** = MCP/能力接入（ServerBuilder）；**HappyClaw R19** = 认证与会话管理（Web 后台登录）；**HappyClaw R20** = RBAC 三态+工作区所有权。
> - HappyClaw 原 R25（行为评测底座）已于 2026-08-24 移除，评测降为轻量业务 Review+典型 Case 回归。
> - MiniCode/Craft/Happy 的 R 编号**各项目独立**，不复用——换项目必须切对应 `references/<项目>/`。

---

## 1. 模型分工（铁律：强模型只碰 4 个点）

| 模型 | 负责 | 铁律 |
| --- | --- | --- |
| **便宜 flash** | 步1打底、步4温和审查、步5 Q&A、步7 PDF | 让它**详细慢慢来**，量大不心疼 |
| **强 Sonnet** | 步2生成场景、步3对齐改写、步4狠版审查、步6多项目联动 | 只碰这几个高杠杆点，其余别用它 |
| **Opus** | 仅步6需要极致时 | 平时不用 |

---

## 2. 每项目 5 步链（三项目各跑一遍，共 15 步）

| 步 | 做什么 | 模块 | 模型 | 产出 / 检查 |
| --- | --- | --- | --- | --- |
| 1 | 打底：按选点逐字抽四段式 bullet + 项目描述四字段 | Resume 模式 | flash | 四字段描述 + 各 bullet（标 Rxx/Scope/Risk）+ 对应 Q 编号 + 核对表 |
| 2 | 生成场景：场景标签扩成【公司画像+JD职责+痛点+关键词】 | Scenario Step 1a 解构 | **Sonnet** | 场景包（含"合理假设"标注，不编精确数字） |
| 3 | 对齐改写：把 bullet 业务问题槽换成场景痛点，保持四段式 | Scenario Step 1a 核心改写 | **Sonnet** | 场景映射表 + 改写 bullet(标 Rxx) + talking points(四段式) |
| 4 | 对抗审查：扮 HR 逐条找漏洞（5 项清单） | 对抗式审查 | flash(温和) / Sonnet(狠版) | 问题清单 位置/类型/严重度/说明/修订建议 |
| 5 | Q&A 预测：取对应 Q 编号 + 弱点追问与降级路径 | Q&A Prediction Step 1b | flash | 只要 Part1 + Part3，标来源 |

**终章（三项目都跑完再做）**

| 步 | 做什么 | 模块 | 模型 |
| --- | --- | --- | --- |
| 6 | 三项目串成能力演进线（实习→工程化，个人→多端架构，个人→多用户数字员工） | Story 模式 Module 7 多项目联动 | **Sonnet**（极致用 Opus） |
| 7 | 做可编辑 HTML / PDF | ASu 命令 / resume-html.md | flash |

---

## 3. 复用人设块（每条消息开头粘贴，省 token）

```text
【三项目人设】
背景：二本秋招，目标岗位 AI 应用开发，TS/MCP 补课中，熟 Agent 基本流程不熟 MCP。
掌握度：L2 ｜ 风险均衡：论文数字只进 talking、严禁进 bullet；未实现不写成已实现；不编造量化指标。
Skill：.claude/skills/paidgroup-agent-resume-skill/ → references/<当前项目>/
```

---

## 4. 每步提示词模板（填空，直接复制给模型）

### 步1 · 打底（flash）
```text
【通用】以 .claude/skills/paidgroup-agent-resume-skill/ 为准，执行 <项目名> 分支 Resume 模式（workflow.md）。
【身份】<实习项目/个人项目>
【选点】<Rxx, Rxx, Rxx, Rxx, Rxx>
【掌握度】L2 ｜ 风险均衡：论文数字只进 talking、严禁进 bullet；未实现不写成已实现；不编造量化指标。
【任务】只做打底，不生成场景、不深挖 Q&A：
  ① 项目描述四字段（项目名称/一句话简介/技术栈/项目定位）——定位字段按【身份】写
  ② 上面选点的四段式 bullet 逐字引用 resume-points.md 原文，每条标 Rxx + Scope Boundary + Risk 等级
  ③ 每条对应 Q 编号
  ④ 把每条列成核对表（四段式是否完整 | 验证效果是否定性 | Scope 是否越界 | 是否 roadmap 写成已实现 | 对应 Q 编号），供我逐条勾
【格式】标题清晰、别省略，我要详细核对。
```

### 步2 · 生成场景（Sonnet）
```text
【通用】接上，<项目名> Scenario 模式 Step 1a 的解构/行业回退。
【场景标签】<企业智能办公助手 / 企业 AI 助手平台 / 跨境电商 AI 数字员工>
【选点】<Rxx...>
【任务】我暂无真实 JD。请按"行业通用痛点 + 合理假设"生成场景包：
  ① 目标公司画像（行业/业务模式/规模/协作方式/技术栈偏好/用的 IM）
  ② 岗位 JD（3-5 条职责 + 任职要求 + 关键词）
  ③ 业务痛点 3-5 条（每条标"合理假设"，不编精确数据，用"目标/预期"）
  ④ 关键词→热点映射：是否有【评测】→<对应评测点>、【技能沉淀】→<对应>、【安全】→<对应>
每条痛点后标注对应到哪个 R 点。
```

### 步3 · 对齐改写（Sonnet）
```text
【通用】对步1 的 bullet 做 Scenario Step 1a 核心改写：
把每条 bullet 的"业务问题槽"换成步2 场景痛点，保持四段式；验证效果用"目标/预期"（除非场景包给了数字）。
严格 Scope Boundary：不点名未入选 R 点的组件名；外部能力（业务API/IM）标自建适配器。
输出：场景映射表 + 改写后 bullet（每条标 Rxx）+ 对应 talking points（四段式）。
```

### 步4 · 对抗审查（flash 温和 / Sonnet 狠版）
```text
【任务】对上面改写结果做对抗式审查（5 项清单：缺口/逻辑冲突/能力归属/场景覆盖/定位完整性），
逐条给 [位置 / 类型(漏洞|冲突) / 严重度(高|中|低) / 说明 / 修订建议]，无问题的也标"通过"。
（狠版：新建 subagent 扮资深面试官独立审查，别自己审自己。）
```

### 步5 · Q&A（flash）
```text
【任务】基于选中 R 点，按 interview-qa.md 取对应 Q 编号 + 弱点追问与降级路径。
只要 Part1（高频题+四段式答）+ Part3（弱点追问+降级路径）。每条标来源 [R点基线]/[JD预测]。
```

### 步6 · 多项目联动（Sonnet/Opus）
```text
【任务】Story 模式 Module 7：把 MiniCode(实习→工程化基础) / Craft(个人→多端 Agent 架构) / HappyClaw(个人→多用户数字员工) 串成能力演进线。
输出：能力演进时间线 + 方法复用线（状态机/工具 Schema/Trace/Eval 如何跨项目迁移）+ 3 分钟"为何适合目标岗位"。
红线：三项目若非同源，只连能力成长，不共享数据库/代码/服务；每模块标【本人实际参与】/【项目整体架构】/【待确认】。
```

### 步7 · 简历文件（flash）
```text
【任务】把定稿文案做成可编辑 HTML（resume-html.md 流程），浏览器打印另存 PDF（A4/背景开/页眉页脚关/100%）。
```

---

## 5. 待确认红线（决定 步2/3/6 能否站住）

1. **HappyClaw"评测点"按 R25（行为评测与可观测底座）** 填——已确认，除非要换 R26/R27。
2. **Craft 用 R10（AgentEvent 统一事件流与生命周期）**，场景=个人深度研究/信息分析工作台（2026-08-29 由 R24 换入；R24 机器降级为复现文档 OPTIONAL）。确认方向即可。
3. **MiniCode 实习身份 + "企业智能办公助手"场景**：开跑前先定真实锚点（职位/公司/时间三证据 + Story Module2 业务故事：业务用户是谁、原来怎么工作）。锚点未定，步6 先按"个人学习项目"世界观跑，能站住再升格。

---

## 6. 执行纪律

- 每步**一条独立消息**：做完上一步，把产出贴给下一步（同线程，上下文不断），别另开聊天窗。
- **切模型用 `/model`**：步1 flash → 步2/3 Sonnet → 步4/5 flash → 步6 Sonnet → 步7 flash。
- 三项目**分开跑**，别一次 Full-flow 全出（输出爆炸、后续每轮背巨型上下文）。
- 便宜模型那几步**让它慢慢来**：把核对表列全给我勾，别急着要漂亮句子。
