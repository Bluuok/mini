# MiniCode 项目定义 · 基准（后续生成唯一事实来源）

> 定位用户已锁死（2026-08-24）。后续 Step 3、JD、Q&A、简历 bullet、场景映射，全部以本文件为基线。改任何一条必须回本文件。
> ⚠️ 与本文件冲突的旧内容（如技能里 Coding Agent / "企业智能办公助手"）一律以本文件为准。

---

## 一、项目定位

- **项目名称**：研发项目智能协作助手
- **项目性质**：AI 应用开发 / Agent 应用项目
- **使用场景**：企业研发团队内部的项目资料与文档协作
- **核心用户**：研发人员、产品人员、项目负责人

暂不细化：公司、部门、团队人数。

---

## 二、边界（不能做成的东西）

1. **不是 RAG 项目**——不实现 Embedding/Vector DB/Chunking/Retriever/Reranker/Hybrid Search/企业知识库检索体系。
   这里的"搜索"= **Agent 通过受控文件/目录工具主动查找项目文件并读取内容**，不要包装成 RAG。
2. **不是 Coding Agent**——不定位成 Claude Code/Cursor/Copilot/AI 编程助手/自动写代码 Agent。
   "研发团队"只是使用背景。Agent 操作对象是：**项目文档、会议纪要、项目周报、需求说明、测试记录、项目资料**，不是源代码开发。
3. **不是企业级通用办公平台**——不扩展到 OA/CRM/ERP/HR/财务/审批/企业 IM 平台。
   只解决一个窄问题：**研发项目资料与文档的查找、整理、生成与受控修改**。

---

## 三、核心业务闭环（固定）

```
用户提出多步骤文档任务
  → Agent Loop 判断下一步动作
  → 调用受控工具
  → 获取文件/搜索结果
  → 模型根据工具结果继续推理
  → 必要时继续调用工具
  → 生成最终结果或文件修改方案
  → 若涉及写文件：生成 Diff
  → 用户确认
  → 批准后真正写入
  → 保存 Session / 执行轨迹
  → 后续可 Resume / Fork
```

---

## 四、典型业务任务（4 类，优先围绕，别无限增加）

1. **项目周报整理**：整理本周项目资料，找会议纪要与进度记录，汇总完成/延期/下周计划，生成周报。→ Agent 搜索、阅读、整理、生成。
2. **会议纪要整理**：找到会议纪要，提取待办事项，按负责人和截止时间整理。→ Agent 定位、读取、提取、输出。
3. **项目文档修改**：把周报的项目状态更新成最新进度、补充本周新增风险。→ Agent 找文档、读原文、生成修改方案、算 Diff、用户确认、写入。**重点对应 R10**。
4. **跨文件信息查找**：找出项目最近一个月所有关于支付模块延期的问题并汇总。→ Agent 搜索多文件、分批读、综合、返回结论。**重点对应 R09 + R01**。

---

## 五、最终选点（固定，勿替换）

**R01 + R08 + R09 + R10 + R18**

- **R01 · Agent Loop 执行闭环**：模型推理 / Tool Call / 工具执行 / Tool Result / 再推理 / 最终回答 / 最大步数终止。
  价值：解决多步骤办公任务无法由单次 LLM 调用完成的问题。
- **R08 · ToolDefinition / ToolRegistry**：工具围绕 `list_files` / `search_files` / `read_file` / `write_file` / `apply_diff` 组织。
  重点：统一工具定义、JSON Schema 参数约束、工具注册、运行时参数校验、工具执行。**不要扩展成复杂 MCP Tool Ecosystem**。
- **R09 · 文件与搜索工具**：目录列举 / 文件搜索 / 分页读取 / 路径安全 / 文件大小限制 / 输出截断。
  目标：**受控地找到本地项目资料**。不要实现专业搜索引擎，不要包装成 RAG。
- **R10 · 写入前 Diff 审查**：**核心安全闭环**。flow = Agent 提出修改 → 生成 Diff → 用户查看 → Approve/Reject → Approve 才真正写入。
  重点讲：**为什么 Agent 不能直接覆盖文件**。不引入复杂安全沙箱。
- **R18 · Session 持久化/恢复/分支**：保存执行过程、保存 Tool Call / Tool Result、支持 Resume / Fork / 基本回放。
  优先 **JSONL 事件流**，不要自动升级成复杂数据库架构。

---

## 六、项目架构层次

```
            用户
             ↓
      Agent Application
             ↓
        Agent Loop
             ↓
       ToolRegistry
             ↓
  ┌──────────┼──────────┐
  ↓          ↓          ↓
文件列表    搜索       读取
                         ↓
                     文档内容
                         ↓
                   模型继续推理
                         ↓
                    修改请求
                         ↓
                    Diff Review
                         ↓
                  Approve / Reject
                         ↓
                       写入

              Session / JSONL
                    ↕
              Resume / Fork
```

**不要主动添加**：Multi-Agent / RAG / MCP / Skill System / Context Compression / Memory System / Agent Evaluation Platform / Sandbox / Complex Permission Lifecycle。

---

## 七、与"实习"的关系

设计成：**企业研发团队实际存在的一类内部 AI 效率工具**。

当前阶段**不要确定**：公司名 / 公司行业 / 公司规模 / 部门名称 / 团队人数 / IM 类型 / 真实客户数量 / 用户数量 / QPS / 性能提升数据 / ROI / 节省人力百分比。

以上全部标记 **【待补充真实实习信息】**，不要编造。

---

## 八、业务合理性原则（每设计一个功能必答 7 问）

1. 谁使用？
2. 什么场景使用？
3. 输入是什么？
4. Agent 做了什么？
5. 为什么需要 Agent？
6. 如果失败怎么办？
7. 对应哪个 R 点？

如果一个功能只是为了"显得高级"→ 删除。

---

## 九、面试深度控制

MiniCode 是三个项目里**最需要能扛面试深挖**的项目。
要求：业务细节具体、技术实现能落到代码层、数据流清楚、**R01/R08/R09/R10/R18 源码级解释**。
但**不引入额外高阶技术债**。面试官应主要围绕：Agent Loop / Tool / 文件访问 / Diff / Session。
**不要主动把面试引向**：RAG / MCP / Skill / Context Compression / 多 Agent / Agent Safety Platform / Evaluation Framework。

---

## 十、一句话定位（最终印象）

> 这是一个**实习生参与实现的企业内部研发项目资料与文档智能协作 Agent**，它通过 **Agent Loop、受控工具、文件搜索、Diff 审查和 Session 持久化**，把**多步骤文档任务**做成了一个**可执行、可控、可恢复的 AI 应用**。

---

## 十一、分阶段锁定内容

- **当前阶段只锁**：项目定位 + 业务场景 + 5 个 R 点 + 核心业务流程。
- **后续阶段再生成**：场景映射 → JD → 简历 Bullet → Q&A → 最终简历。
- **最后阶段再补**：真实实习公司画像（公司名/行业/部门/团队/IM/实际协作方式/真实职责）。
