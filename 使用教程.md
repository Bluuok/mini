# 三套付费版简历内参 · 使用教程

> 本文面向第一次拿到 `minicode付费版内参`、`happyclaw付费版内参`、`craft-agents付费版内参` 三套资料的会员。
> 目标是：**10 分钟内知道每套资料是什么、每个文件干什么、怎么快速生成一版能写进简历、能扛住面试追问的项目经历。**

---

## 一、这套资料是什么

三套资料 = 三个真实可讲的 AI Agent 项目素材库。每套都做成两种形态：

- **人读版内参**：你自己阅读、选点、改简历、背面试题（Markdown 文档）。
- **Agent Skill 版**：把 Skill 装进 Claude Code / Codex 等工具，让 Agent 根据你的背景自动生成简历项目 + 面试准备清单。

核心承诺：给你一个**能写进简历、能讲清技术、能准备追问**的 AI Agent 项目。

三套共用同一套编号体系，学一次三套通用：

| 编号 | 含义 | 例子 |
| --- | --- | --- |
| `Rxx` | 简历要点（Resume Point） | `R01` Agent Loop 执行闭环 |
| `Qxx.x` | 围绕要点的面试追问 | `Q01.1` `Q01.2` |
| `Wxx` | CodeWiki 深挖索引 | `W01` 核心代理循环章节 |

写进简历的每个 `Rxx`，都必须能在 Q&A 和 CodeWiki 里找到对应依据。

---

## 二、三套怎么选

| 内参 | 项目形态 | 适合岗位方向 | 要点数量 |
| --- | --- | --- | --- |
| MiniCode | 轻量终端 AI Coding Agent（类 Claude Code 参考实现） | AI 应用开发、后端、CLI 工具链、Agent 工程 | R01-R25（25 个） |
| HappyClaw | 多用户 Agent 工作台 + 企业数字员工平台（7 渠道 IM） | 企业 AI 应用、数字员工、Agent 平台、前端 | R01-R27（27 个） |
| Craft Agents | 多端 Agent 架构（Electron/WebUI/CLI/Server） | Agent 架构、AI Infra、后端平台、桌面端 | R01-R27（27 个） |

选择建议：

- 目标是「AI 应用 / 后端 / 工具链」→ 优先 MiniCode，难度最低、最好讲。
- 目标是「企业级 Agent 平台 / 数字员工 / IM」→ 用 HappyClaw，故事最完整。
- 目标是「Agent 架构 / 高薪平台岗」→ 用 Craft Agents 或 HappyClaw 的高阶组合。
- 不确定 → 先看每套的 `04-简历组合方案`，三套都读一遍「方案 A」和「方案 H/I」再决定。

---

## 三、每套内参的文件地图

三套结构完全一致，以 MiniCode 为例：

```
minicode付费版内参/
├── 00-MiniCode双产品MVP说明.md      # 产品定位：这套资料是什么、怎么用、硬规则
├── 01-MiniCode项目使用指南-付费版.md # 详细使用指南（新手先读这个）
├── 02-MiniCode简历要点库-付费版.md   # 简历要点库：25 个 R 点，选 4-5 个写进简历
├── 03-MiniCode项目面试Q&A-付费版.md  # 每个 R 点对应的面试追问与回答
├── 04-MiniCode简历组合方案-付费版.md # 按岗位/基础预配好的组合方案（A-I）
├── MiniCode场景对齐提示词.md        # 贴 JD 场景化时直接复制使用的提示词
├── minicode-resume-skill/          # Agent Skill 版（装进 Claude Code/Codex）
│   ├── SKILL.md                    # Skill 入口（Agent 工作流）
│   ├── agents/openai.yaml          # Codex 的 Agent 配置
│   └── references/                 # 要点库/QA/组合方案/CodeWiki 压缩库
└── minicode-skill-example/         # Skill 真实执行示例（跑完长什么样）
    ├── 00-完整执行示例.md           # 一次完整对话过程
    ├── 01-Agent产物输出.md          # 生成的简历项目长什么样
    └── 02-评测记录.md               # Skill 效果评测（部分套装有）
```

各文件使用优先级：**`04`（选方案）→ `02`（挑要点）→ `03`（背追问）→ `01`（细读规则）→ `00`（理解定位）**。

---

## 四、五分钟快速上手

### 路线 A：纯手动（不想用 Skill）

1. **选方案**：打开 `04-简历组合方案`，按你的岗位和基础挑一个方案（新手选 A，多数人选 G/H，冲高薪选 I 前沿热点版）。
2. **挑要点**：打开 `02-简历要点库`，从方案推荐的 5 个点里选 4-5 个你能讲清的 `Rxx`。**不要贪多**。
3. **写简历**：把选中的点改写进简历，格式固定为四段式：

   > **项目简介一句话 + 每条要点按「针对xx业务问题，基于xx技术，设计了xx工程方案，实现了xx验证效果」**

4. **背追问**：打开 `03-项目面试Q&A`，找到每个 `Rxx` 对应的 `Qxx.x`，按四段式准备回答：解决什么问题 → 怎么实现的 → 为什么这样设计 → 局限与扩展。
5. **自查**：每个写进简历的点，你都要能不看资料讲出它的数据流；讲不清就换点或降级。

### 路线 B：用 Skill 自动生成（推荐）

1. **安装**：把 `minicode-resume-skill`整个文件夹复制本地然后让agent加载这个skill到全局skill文件夹：

   > 请使用 MiniCode Resume Skill，把 MiniCode 包装成适合我背景的简历项目。
   > 我的背景：二本秋招。目标岗位 AI 应用开发，技术栈 TypeScript（实际不熟），有java后端基础但在这里最好不要体现。熟悉 Agent 基本流程和工具调用，不熟 MCP 协议细节。我要走i评测路线，我还没学，先假定我都能讲清楚。

2. **Agent 会输出**：项目名称、一句话项目简介、技术栈、4-5 条四段式简历 bullet、每条对应的 Q&A、高风险点与降级建议。
3. **不满意就追加要求**：换方案、降级某点、基于 CodeWiki 深挖某点、生成项目故事、对抗式审查简历漏洞。

### 路线 C：贴 JD 场景化（有目标公司/岗位时）

1. 复制对应套装的 `XX场景对齐提示词.md`，把 JD 和公司背景贴进去，发给 Agent（或直接让 Skill 进入场景模式）。
2. Agent 会输出：场景映射表（JD 需求 → 项目能力）、重写后的简历 bullet（贴公司业务）、面试 talking points、落地增强方案。
3. 场景化只改叙述框架，**不改变能力层级**——JD 没给的数字不要编，验证效果用「目标/预期」表述。

---

## 五、面试前必备规则（防穿帮）

1. **只写 4-5 个点**：写 10 个技术点 = 10 个追问靶子。
2. **讲不清就不写**：高风险点（各套装的 `R04/R16/R23` 类）需要源码级理解，不行就按方案里的降级路径换点。
3. **不编造指标**：性能数据、用户数、业务收益、线上事故，一律不写。
4. **路线图不算已实现**：CodeWiki 里的 ROADMAP/扩展设想不能写成已完成能力。
5. **前沿点区分两层**：评测/自进化/安全类前沿点（各套装的 R23-R27），【项目实现】进简历 bullet，【前沿认知】（论文数据，如 +16.6pp、15%→92%）只作为面试展开材料，两者混用必然穿帮。
6. **回答用四段式**：解决问题 → 实现方式 → 设计取舍 → 局限与扩展；追问答不上来就用降级路径，诚实比硬编安全。

---

## 六、常见问题

**Q1：我不会写代码，能用吗？**
能。人读版内参不需要写代码：按 04 → 02 → 03 的顺序选点、改简历、背 Q&A 即可；Skill 版也只需要你在对话里描述自己的背景。

**Q2：三套可以混着写吗？**
不建议把三套方案简单拼接成一个项目（追问面过大）。跨项目只串联能力成长线，不编造共用的系统。一般选一套主打即可。

**Q3：Skill 装好了但 Agent 没反应？**
确认文件夹名与路径正确（Skill 目录里必须直接是 `SKILL.md`），新开会话，并用「请使用 XX Resume Skill…」开头明确触发。

**Q4：生成的 bullet 太高级，讲不动怎么办？**
要求 Agent「降级表达」：把高阶点替换成方案里的中低风险点（如 `R23` 讲不稳 → 降级 `R10`），或要求「降低技术措辞」。

**Q5：面试被问到简历里没写的点怎么办？**
诚实说明边界 + 给出设计思路，不要现场编。各套装的 Q&A 里已为高风险点预置了「扛不住时怎么答」的降级路径。

**Q6：怎么判断自己选的点稳不稳？**
对每个点做 30 秒电梯测试：能不看资料讲清「解决什么问题 + 怎么实现 + 边界在哪」。讲不清 = 换点。
