# Craft Agents 定稿工作文件（步4 修订版 · 选点 v3：R24→R10）

> 项目：Craft Agents（个人项目）｜场景：个人深度研究/信息分析工作台｜选点 R03 R08 R10 R12 R18
> 选点变更记录（2026-08-29）：原选点 R03 R08 R12 R18 R24，**R24（分层验证体系）→ R10（AgentEvent 统一事件流与生命周期）**。理由：R24 是 Low 支撑点、故事性弱；R10 与 R03 同宗——R03 讲「后端接口统一」，R10 讲「事件契约统一」（流式渲染/错误恢复），主次分明可深挖；R10 对研究场景原生契合（长链路执行可见性+外部来源易错的恢复）。R24 机器降级为复现文档 OPTIONAL（用户裁决）。
> **范围控制（2026-08-30 范围裁决）**：依赖关系 **R03→R10**（R10 消费 R03 的事件产出、定义契约）；**R08、R18 相互独立**（与事件层仅在运行时汇合）；**R12 最后汇总承载**到多端 UI。简历顺序按 JD 镜像（R03→R08→R18→R12→R10，C 终裁）；实现施工顺序 R03→R10→R08→R18→R12 线性排——**这是施工顺序，不是严格代码依赖**，被追问依赖结构按本句答。五个点是「Agent 工程五个切面」，不是半个 Agent Runtime；未选点不得实现、不得形成可调用入口。
> 本文件是步4 四审查员（叙事A/归属B/覆盖C/终审D）裁决后的修订版。步5 Q&A 基于此。
> 红线：验证效果全定性；不虚构使用主体/业务反馈；未实现不写成已实现。
> ⚠️ 编号铁律：本文件内 R03=后端抽象、R08=Session隔离、R10=AgentEvent 事件流、R12=IPC/WS传输、R18=MCP接入。MiniCode 的 R10=写前确认 diff，与本文件 R10 无关。

## 0. 四字段（步4 修订版 · v3 重锚）

- **项目名称**：Craft Agents
- **一句话简介**：面向深度研究（搜索→阅读→分析→汇总）与信息分析场景的多端 Agent 工作台，把研究任务做成可配置、可运行、可接入、可复用的 AI 应用。
- **技术栈**：TypeScript、React、Electron、Bun/Node（Bun workspace）、WebSocket/RPC、MCP
- **项目定位**：个人项目 · 多端 Agent 应用平台（非 Agent Infra、非 RAG 知识库），聚焦模型后端解耦、研究任务会话隔离、多端传输复用、外部能力统一接入与统一事件流呈现。

> v3 修订记录（2026-08-29）：定位末位「工程验证」→「统一事件流呈现」（R24 移除后失去支撑，重锚到 R10；避免与 R03「模型后端解耦」措辞重复——R03 锚后端解耦、R10 锚事件契约与呈现）。

## 1. 五条 bullet（步4 修订版 · 简历镜像 JD 排序：R03→R08→R18→R12→R10；实现施工顺序 R03→R10→R08→R18→R12，R08/R18 独立成块、R12 汇总承载）

> 排序说明（2026-08-30 范围裁决补记）：简历顺序按 JD 镜像（C 终裁）；**施工顺序按 R03→R10 先行、R08/R18 独立、R12 收尾**——这是施工顺序不是严格代码依赖。两者不矛盾：简历对外讲价值优先级，代码对内讲施工排期。

### R10 基线存档（步1 · 逐字引用 resume-points.md，面试口径一律以 §3 场景化版为准）
针对流式渲染与错误恢复缺乏统一事件契约、UI 解析逻辑分散的问题，基于可辨识联合类型，设计了 AgentEvent 事件流协议（文本增量、工具启动/结果、权限请求、结构化错误与完成状态），实现了 UI 实时渲染与错误恢复的统一。
- Scope：事件流联合类型（核心六类用于流式渲染/错误恢复）；**评测/采样/度量是消费侧能力，非该类型职责**。Medium｜Q10.x｜W03 W07 W14

### R03 · 模型解耦 · AgentBackend 抽象（Medium）
针对深度研究流程不同阶段需要不同模型（信息抓取整理要求快与低成本、分析综合要求强推理能力）、前端耦合单一模型 SDK 导致按任务更换模型或增换模型服务商均需改动整个调用链的问题，基于抽象接口，设计了 AgentBackend 抽象（将 Claude、Pi 等不同模型后端统一为 provider-agnostic 的 chat() 接口），实现了 UI 层与模型供应商的解耦、后端切换收敛为配置层改动。

> 修订记录：问题槽「全链路改造」→「改动整个调用链」、「供应商」→「模型服务商」（A：去 B2B 腔）；效果槽「模型切换收敛到配置层」→「后端切换收敛为配置层改动」（B：避免借用 R07「按任务动态切换模型」语义，限定为后端/供应商层）。Pi 保留（项目真实类名，PiAgent=进程外 JSONL 协议接入的第二后端），已入源码补课清单。
> v3 修订记录（步4 第三轮）：删「基于抽象接口与统一事件流」→「基于抽象接口」；「流式事件接口」→「chat() 接口」（A3/B2/D雷7 三员一致裁决：R03 只讲接口抽象、事件流语义全部移交 R10，消除 R03/R10 叙事撞车——R03 管「谁能被换」，R10 管「换完之后 UI 还认」）。

### R08 · 任务隔离 · Session/Workspace 隔离（Low）
针对多个研究任务并行推进时搜索结果、网页原文、中间结论与偏好配置在单一上下文中互相混杂、结论不可信且任务无法并行推进的问题，基于 Session/Workspace 隔离设计，设计了以 Session 与 Workspace 为边界的状态隔离体系（管理会话生命周期、工作目录、偏好配置与工具上下文），实现了多研究任务的并行隔离（一个研究任务一个 Session 与独立工作区）。

> 修订记录：A 判定通过（真锚点，Context 污染是 Deep Research 公认痛点），基本保持；B 合规（「一个任务一个 Session+独立工作区」在 Scope 内；注意口径用「隔离使任务可并行」，不说「调度编排」）。JD「编排」半缺口走面试口径（见 §5-8）。

### R18 · 能力接入 · 统一能力接入层（High）
针对搜索 API、网页抓取、本地文档、MCP 工具等第三方研究能力各自为政接入、凭据与配置散落、每接入一个能力需改造一遍代码的问题，基于 ServerBuilder 统一转换，设计了统一能力接入层（MCP/API/本地数据源统一转换为 Agent 可调用工具，凭据注入与权限配置集中管理），实现了外部研究能力的统一接入与调用。

> 修订记录：问题槽加「第三方」限定（B：防「平台自带搜索工具」误读，呼应 R17 边界「业务工具体需另行实现」）；效果槽「统一调度」→「统一接入与调用」（D：「调度」是承重词，与「不做多 Agent 编排」防线矛盾；收窄语义合规）。

### R12 · 多端复用 · IPC/WebSocket 统一传输层（Low）
针对研究工作台需同时覆盖桌面端主力工作与 Web 端随时接续、两端各自维护一套会话/文件/模型连接逻辑导致功能不同步、维护成本翻倍的问题，基于统一 RPC 抽象，设计了 IPC/WebSocket 统一传输层（统一 RPC channel、DTO 与路由规则），实现了同一套会话、文件与模型连接逻辑的多端复用。

> 修订记录：A/B 均判合规，bullet 不动。动机弱（个人为何多端）走口径补强（见 §5-5）。禁止追加「跨端无缝续接会话状态」（B：那依赖 R09 共享持久化，非传输层）。

### R10 · 事件契约 · AgentEvent 统一事件流与生命周期（Medium · v3 换入）
针对深度研究任务执行链路长（搜索→阅读→分析→汇总常达分钟级）、执行中文本增量、工具进度、权限确认与外部来源错误（搜索 API 超时、网页抓取失败等）混杂呈现、缺乏统一事件契约导致 UI 解析逻辑分散且错误难以恢复的问题，基于可辨识联合类型，设计了 AgentEvent 事件流协议（文本增量、工具启动/结果、权限请求、结构化错误与完成状态），实现了执行过程的标准化实时呈现与结构化错误的统一恢复入口。

> v3 场景化记录（步3）：问题槽由基线泛化 UI 措辞换成研究原生痛点（长链路执行可见性+外部来源易错恢复），方案槽守基线技术语义（可辨识联合类型+五类事件）。
> v3 修订记录（步4 第三轮）：效果槽「实时呈现与错误恢复的统一」→「标准化实时呈现与结构化错误的统一恢复入口」（C问题2：事件协议提供信息基础与恢复入口，恢复动作由消费侧/UI 决策，不暗示自动恢复；「标准化」与 R03「统一」措辞区分）。「多研究任务并行隔离」bullet 中「并行」保持（C问题3 低危：口径 §5-8 已备，「异步并发非线程并行」）。「可扩展」→「可接入」（C问题4，§0 简介同步）。

## 2. Talking points（四段式：问题背景/实现方案/设计权衡/局限扩展）

### R03
- 问题背景：研究流程天然分阶段——抓取整理要快和省，分析综合要强推理；个人项目成本敏感，按任务选模型是刚需。
- 实现方案：AgentBackend 统一 chat() 接口、配置控制与生命周期管理，后端返回标准化流式事件，UI 层对后端来源无感；换模型=换配置，不是换代码。
- 设计权衡：抽象必然屏蔽后端特有能力——用配置标记 + 运行时能力白名单兜底，不为单家服务商破坏统一接口。
- 局限扩展：后端抽象非完整 Runtime——会话调度、工具分发、权限编排由其他模块承担。扩展方向：新服务商只写一个适配器。

### R08
- 问题背景：单 Agent 把所有研究任务混在一个上下文里，是「深度研究混成一锅粥」的根因——不是模型不行，是状态没隔离。
- 实现方案：一个研究任务一个 Session（会话生命周期、工具上下文）+ 独立 Workspace（工作目录、偏好、数据源），并行任务互不读写。
- 设计权衡：Session 与 Workspace 分开而非合一——同一工作区可跑多个会话，任务结束销毁 Session 不破坏工作区积累的资料。
- 局限扩展：文件系统/工作区级隔离，不是多 Agent 编排，更不是 SaaS 多租户 DB 行级隔离——多任务并行靠多 Session，不靠多 Agent 协作；跨任务知识复用（Memory）未实现，是演进方向。

### R18
- 问题背景：研究能力来源杂——搜索 API、抓取、本地文档、MCP 生态，散装接入意味着每接一个改一遍代码、凭据到处放。
- 实现方案：ServerBuilder 把 MCP/API/本地数据源统一转换为 Agent 可调用工具；凭据按 Source 注入、权限在注册时配置——统一入口即治理边界。
- 设计权衡：Source（连接/凭据/配置）与 Skill（使用逻辑）分离，接入与用法解耦。
- 局限扩展：具体研究工具（搜索聚合、抓取清洗）是自建适配器非平台原生；MCP 接入只讲到统一转换层（L2 口径）；技能自动生成是后续演进方向，不展开。

### R12
- 问题背景：桌面端是主力工作台（长任务、大屏分析），Web 端负责随时接续查看进度——双端是真实使用形态，不是伪需求。
- 实现方案：统一 RPC channel + DTO + 路由规则，Electron IPC 与 WebSocket 只是可替换的传输边界。
- 设计权衡：文件系统能力两端天然不同（桌面本地直连、Web 经服务端代理）——差异收敛在 DTO 与路由层，上层会话/文件/模型逻辑零改动复用。
- 局限扩展：传输层抽象，不是多端 Runtime。扩展方向：新增客户端（如移动端）只需实现传输适配。

### R10
- 问题背景：深度研究任务链路长（分钟级），用户需要全程看到「现在搜到哪、读到哪、分析到哪」；外部来源天然易错（搜索 API 超时、抓取失败、MCP 工具报错），错误不能只是一行红字就断掉整个任务。
- 实现方案：AgentEvent 可辨识联合类型统一事件词表——文本增量（text_delta/text_complete）、工具启动/结果（tool_start/tool_result）、权限请求（permission_request）、结构化错误（typed_error）、完成状态（complete+usage）；UI 按 type 分流渲染，双后端（Claude 进程内/Pi 子进程）事件都映射到同一词表。
- 设计权衡：事件协议是「渲染契约」不是「日志格式」——每类事件带关联字段（turnId 归组一轮、parentToolUseId 挂出嵌套工具树——该字段可选，Claude 后端原生透传、Pi 后端当时用 sub-turn 隔离近似，词表跨后端对齐但字段填充度按后端能力不同），UI 不用自己猜顺序；抽象的代价是后端特有事件要先适配收敛，宁可适配层多写也不让 UI 认识两家方言。
- 局限扩展：这是事件流联合类型与渲染/恢复契约——评测/采样/度量是消费侧能力，不是该类型职责（被问「研究结论质量怎么评估」：行为评测是方法论认知未实现，事件流只是提供了行为可复现的数据基础）；恢复由消费侧（UI/会话层）根据事件类型决策——协议提供结构化信息与统一恢复入口，不是自动重试；「生命周期」指事件间的关联与状态流转（turn 归组/工具树/错误恢复），**不是严格意义的 FSM 状态机**（无显式状态转移表），被追问「状态机怎么建模」就按这个口径画界。R10 依赖 R03 后端提供的事件源——事件契约定义的是中间格式，不是事件源头。

## 3. 场景包要点（步2 版 · v3 微调）
- 使用者=本人：技术调研、论文/文档阅读、方案比较、行业信息汇总；多研究任务并行。
- 形态：Electron 桌面主力 + Web 接续；按任务阶段选模型（快省/强推理）；外部能力=搜索 API/抓取/本地文档/MCP。
- JD 关键词（v3）：任务隔离 · 模型解耦 · 多端复用 · MCP/能力接入 · 流式渲染 · 事件驱动 UI · 错误恢复 · Deep Research（原「契约测试」随 R24 移除，换流式/事件/恢复类，合理假设）。
- 前沿点兜底（v3 重路由）：评测→不选点，诚实交底「方法论认知未实现；事件流只是数据基础」（R10 Scope 明确评测非其职责）；技能治理→R18；安全→R18。均不进 bullet。
- 场景边界（不进 bullet）：不做 RAG/VectorDB；不做多 Agent 编排（多任务=多 Session）；不做复杂 Memory（未实现）；「深度研究」=流程编排非产品复刻；简历不写英文 "Deep Research"（C：硬翻引向 RAG/多Agent，简介中文流程点明即可）。

## 4. 审查记录
### 第一轮（企业场景版，已作废）
- 个人项目披企业皮=系统性风险，触发场景更换。
### 第二轮（研究工作台版 · 四审查员并行）
- A 叙事 7/10：零虚构主体；R24 i18n 高危已删；R12 多端动机需口径。
- B 归属：五条均未越界；R03 效果槽已收敛为后端层。
- C 覆盖：编排半缺口走口径；排序已镜像 JD；Deep Research 不上英文词。
- D 终审 60/100（备好防线 ~75）：Pi 名字雷（保留+补课+防线）；R18「统一调度」已改；MCP 先发自爆已入口径；单人防御叙事已入口径。

### 第三轮（选点 v3 · R24→R10 换点版 · 2026-08-29 · 四审查员并行）
- **变更动因**：R24 是 Low 支撑点、故事性弱；R10 与 R03 同宗（接口统一→事件契约统一）主次分明；R10 对研究场景原生契合（长链路执行可见性+外部来源易错恢复）。R24 机器降级为复现文档 OPTIONAL（用户裁决）。
- **A 叙事 74→85+（修订后）**：叙事骨架可信、零虚构主体；核心裁决=R03/R10 存在实质重叠（R03 bullet「统一事件流」+talking 引用「AgentEvent」把 R10 领地提前讲完）→ 已修订（R03 删事件流措辞、talking 改「标准化流式事件」）；R10 收尾位裁决通过（叙事弧线「构建→整合」完整、避免紧邻对比放大重叠感、首尾效应落用户体验记忆点）。
- **B 归属 87/100**：五条 bullet 归属全部在 Scope 内、组件名源码核实全真（AgentEvent@message.ts:550、EventQueue、双 adapter、DRIVER_REGISTRY 等逐项 ✅）、未选点语义零蹭入；中危 1 处（R03 talking「按任务选模型」须加「配置层手动」限定防回蹭 R07 语义）→ 已修订；低危 6 处（R03/R10 措辞撞车、五↔六类计数口径、效果槽「错误恢复」措辞、R24 组残留 Part1、SourceServerBuilder 精确名、R08「独立工作区」口径）→ 全部处理。
- **C 覆盖 82/100（两个必改已执行）**：六痛点五点全覆盖无硬缺口；必改①R10 效果槽「错误恢复」承诺超能力→降级「结构化错误的统一恢复入口」+局限扩展明确消费侧决策；必改②工程素养维度缺 bullet 支撑→R10 局限扩展嵌「事件流结构化日志→行为可复现/回归可定位」工程价值表述+§5-9 保留口径兜底；低危「并行」微措辞（口径已备）与「可扩展→可接入」已改；排序终裁=收尾（JD 新增亮点关键词最突出+叙事弧线完整+首尾效应）。
- **D 终审 68→82（备好防线后）**：换点成立（R10 显著优于 R24）；七类盘问雷全列（事件类型五↔六↔21 型口径/turnId 关联/typed_error 恢复/双后端一致性/EventQueue 反换皮/评测越界/R03-R10 分工）；最高优先缺口=「换了好枪没配子弹」（R10 Q&A 组、专属防线、扩展事件口袋全空）→ 本轮已全部补齐（§5-R10 防线、§7 R10 组、Part3 升格）；parentToolUseId 诚实限定（Claude 原生透传/Pi sub-turn 近似）已写入 talking；permission_request 出处纠正（SessionManager 安全管线发出，非 adapter 产物）已入防线。

**v3 采纳清单**：R03 bullet 删「统一事件流」+「流式事件接口」改「chat() 接口」；R03 talking 改「标准化流式事件」+删 AgentEvent 专名引用；R10 效果槽降级「结构化错误的统一恢复入口」；R10 talking 补 parentToolUseId 诚实限定+依赖 R03 事件源声明+消费侧恢复边界；简介「可扩展→可接入」+删流程罗列；§0 定位字段重锚；新增 §5-R10 防线（事件词表+turnId+EventQueue+permission_request 出处）；§5-9 保留+工程价值句；§6 补课#5 换 R10；§7 R24 组→R10 组+Part3 更新；Q03.1 实现措辞改「标准化流式接口」。

## 5. 面试防线口径（步4 产出 · 背熟 · v3 更新）

1. **Pi 防线（最重要）**：Pi 是一个开源 agent runtime，Craft 把它作为第二个模型后端，通过**进程外 JSONL 子进程协议**接入（独立进程隔离重依赖，工具调用代理回宿主）。统一事件流=结构化可辨识联合类型：文本增量、工具启动/结果、权限请求、结构化错误、完成状态——不是 async generator 包一层。→ 源码补课清单+1（PiAgent 接入 + R21 JSONL 协议）。
2. **MCP 先发制人自爆**：第一句主动讲「我的 MCP 只到统一转换层（把 MCP server 的 tool 转成 Agent 可调用函数签名），JSON-RPC/握手/传输层没深做」——被问前说是诚实界定，被问后说是逃避。
3. **为什么不用一个强模型全程跑**（R03 反杀）：个人项目自费 API 成本敏感；抓取/整理占 token 大头，用强模型浪费；**我在配置层**为不同研究阶段选不同模型（抓取用快省、分析用强推理）——是成本/质量权衡，不是为抽象而抽象。被问「系统怎么自动切」→「当前是配置层选择，不是自动路由」（B：防回蹭「按任务动态切换模型」语义）。
4. **多任务并行 vs 多开窗口**（R08 反杀）：隔离的是工具上下文/工作目录/偏好配置，不只是 UI 窗口；共享同一份上下文/工作目录的多窗口，搜索结果和中间结论照样串。（「并行」=异步并发非线程并行，隔离保障的是状态不串。）
5. **多端真话**（R12）：桌面为主，Web 端是验证「同一套会话/模型逻辑能否复用」的第二个载体——重点不是两个产品，是传输层只写一遍。真实场景：不在电脑前用网页端接续看任务进度/补结论。
6. **单人防御叙事（v3 重锚 R10+R12）**：「我一个人维护、迭代快，更需要把契约和边界锁死，否则 bug 追不回。事件契约、传输路由这些边界一次锁对，后面高频改的是实现不是契约。这不是团队规模决定的，是单人高频迭代决定的。」——全场最好用的化解牌。
7. **为什么不做 RAG**：「我主打实时抓取和多源核对，离线语料检索不是主诉求。场景选择，不是缺陷；要是做语料密集型研究，就切 RAG。」
8. **编排缺口**（R08/JD）：「任务粒度即 Session 粒度，编排只到会话级（生命周期管理），不做 Agent 间编排。」
9. **「工程验证」说人话（v3 保留+工程价值句）**：「保证我频繁改代码时不会把自己写崩。」+ 事件契约的工程价值：「事件流结构化之后，每轮执行可回放、回归问题可定位——这是行为可复现的数据基础，不是行为评测本身。」
10. **R10 事件契约防线（v3 新增，背熟四件套）**：
    - **五类口径**：面试只讲五类（文本增量/工具启动结果/权限请求/结构化错误/完成状态）——「五类是内容事件的类别口径，完成状态是收尾态；底层是可辨识联合类型，类型定义里还有状态提示、用量更新这类辅助事件，按需扩展」（B/D：五↔六↔21 型口径调和，防「就这？」也防背全表）。
    - **turnId 关联**：一轮事件用 turnId 归组（Claude 取 SDK 消息 id），parentToolUseId 让嵌套工具挂对父节点、UI 画树不画平铺时间线；**该字段可选——Claude 后端原生透传，Pi 后端当时用 sub-turn 隔离近似**（D雷2 诚实限定：词表跨后端对齐，字段填充度按后端能力不同，UI 兼容可选字段）。
    - **EventQueue 不是换皮**：Claude 是同步 for-await 消费 SDK 流；Pi 是子进程异步事件回调——EventQueue 把 push 变 pull（enqueue 推入+drain 消费+complete 收流+reset 翻新），让双后端都出同一个 AsyncGenerator 接口（D雷5：缓冲/完成信号/生命周期，纯换皮只需要 yield）。
    - **permission_request 出处**：不是事件适配器发的——是会话层安全管线在工具执行前拦截后发出（D雷3 纠正：PreToolUse 拦截→permission_request→用户裁决→放行或记 block reason）；typed_error 五字段（code/title/message/actions/canRetry）+ 一个真实例子讲清恢复入口。
11. **R03 vs R10 分工一句话（v3 新增）**：「R03 管『谁能被换』——后端接口统一，换后端=换配置；R10 管『换完之后 UI 还认』——事件词表与渲染/恢复契约统一。接口出流，流有词法。」（D雷7/A3：被问重复时先分层位再答实质。）
12. **评测越界反问预案（v3 新增）**：被问「事件流都能回放了，怎么不做评测？」→「评测管线=采样策略+判定标准+裁判模型三个独立问题，每个都是一整块活；事件流是行为可复现的数据基础，评测是消费侧能力，不在这个类型职责内。我没实现就说没实现。」（D雷6：诚实交底本身是加分。）

## 6. 源码补课清单（用户要求带读 · v3 更新）
1. R18 ServerBuilder（源码类名 SourceServerBuilder，sources/server-builder.ts:78）：MCP/API/Local Source → 工具的转换路径 + 凭据注入 + 权限配置（最高优先，步5 Q&A 主战场）。
2. R03/R05 PiAgent：进程外 JSONL 协议接入、事件映射、工具代理（Pi 防线）。
3. R10 事件流（v3 新增，第三优先）：core/types/message.ts AgentEvent 联合（五类核心+辅助型扫一遍）、双后端 event-adapter（Claude pendingText 延迟/Pi 映射表）、EventQueue 四方法、typed_error 结构与一个真实恢复例子、session-event-message parity 测试怎么写的。
4. R08 Session/Workspace：生命周期管理与隔离边界实现。
5. R12 统一 RPC：channel/DTO/路由，桌面直连 vs Web 代理的分叉点（路由穷举测试是 R12 灵魂细节，顺带看）。

## 7. Q&A 预测（步5 · Part1 高频题 + Part3 弱点降级）

### Part 1 · 高频题（四段式答：问题/实现/权衡/局限）

**R10 组（v3 新增）**
- Q10.1 [R点基线] 为什么需要统一事件契约？→ 问题=流式渲染与错误恢复缺统一契约、UI 解析逻辑分散；实现=AgentEvent 可辨识联合统一事件词表（五类核心），UI 按 type 分流渲染，双后端事件都映射到同一词表；权衡=渲染契约 vs 日志格式（事件带 turnId/parentToolUseId 关联字段，UI 不猜顺序）；局限=评测/采样/度量是消费侧能力非该类型职责，恢复由消费侧决策。
- [JD预测] 事件具体有哪几类？字段长什么样？→ 五类口径（文本增量/工具启动结果/权限请求/结构化错误/完成状态）；text_delta 带 turnId 关联；typed_error 结构化（code/title/message/actions/canRetry）→ 错误卡+重试入口。深挖辅助事件→防线 §5-10 口径（类别+用途，不背全表）。
- [JD预测] 错误之后任务还能继续吗？→ 看错误类型：结构化错误带 canRetry 与建议动作，恢复由会话层/UI 按类型决策（重试/重连/引导配置）；协议提供信息基础与统一恢复入口，不是自动恢复（C 必改口径）。
- [JD预测] 双后端事件形状不一样怎么统一？→ 共享适配基座（turn 生命周期/共享状态），各后端一个适配器映射到统一词表（Pi：message_update→text_delta 等）；结构性差异收敛在适配层（Claude 同步流/Pi 异步 EventQueue 桥）。
- [JD预测]（衔接 R03 组）R03 说了统一事件流，你这不重复？→ 防线 §5-11：「R03 管『谁能被换』，R10 管『换完之后 UI 还认』。」

**R03 组（v3 措辞同步）**
- Q03.1 [R点基线] 为什么做模型后端抽象？→ 问题=UI 耦合 SDK 换后端改整个调用链；实现=chat()/配置控制/生命周期/标准化流式接口统一；权衡=抽象屏蔽差异 vs 特有能力透传（配置标记+运行时能力白名单）；局限=后端抽象非完整 Runtime，会话调度/工具分发/权限编排由其他模块承担。
- [JD预测] 为什么不用一个强模型全程跑？→ 个人项目自费 API 成本敏感；抓取/整理占 token 大头用强模型是浪费；**我在配置层**为不同阶段选不同模型，是成本/质量权衡，不是为抽象而抽象（被问自动切换→「配置层选择，非自动路由」）。

**R08 组**
- Q08.1 [R点基线] Session 和 Workspace 区别？→ Session 隔离会话/工具状态（生命周期、工具上下文）；Workspace 定义环境（工作目录、数据源、偏好）。同一工作区可跑多会话，销毁 Session 不破坏 Workspace 资料。
- [JD预测] 多任务并行和多开窗口有什么区别？→ 隔离的是工具上下文/工作目录/偏好配置，不是 UI 窗口；共享同一上下文/目录的多窗口，搜索结果和中间结论照样串。
- [JD预测] 要支持多用户/多租户怎么办？→ 主动交边界：文件系统/工作区级隔离；行级权限需上层 DB/RLS，平台不提供——需要时在上层补，不在本模块硬撑。

**R18 组**
- Q18.1 [R点基线] Source 和 Skill 区别？ServerBuilder 干什么？→ Source=连接/配置/凭据；Skill=使用逻辑；ServerBuilder 把 MCP/API/Local Source 统一转换为 Agent 可调用工具，凭据按 Source 注入、权限注册时配置。
- [JD预测] MCP 是什么、你怎么接的？→ 先发自爆：「我的 MCP 只到统一转换层——把 MCP server 暴露的 tool（带输入 schema）转成 Agent 可调用函数，JSON-RPC/握手/传输层没深做」。然后讲统一入口/凭据/权限的治理价值。
- [JD预测] MCP tool 和普通函数调用的区别？→ tool 带 schema 声明、运行时发现（tools/list）、跨进程托管（server 独立进程）；普通函数编译期绑定。L2 讲到这层即可。
- [JD预测] 不做 RAG，那 R18 解决什么？→ 接入治理问题（统一入口/凭据/权限），不是检索问题；检索类能力作为外部工具接入，平台不内置。

**R12 组**
- Q12.1 [R点基线] 为什么统一 IPC 和 WebSocket？→ Electron 与 WebUI 共享会话/文件/模型逻辑，只换传输边界；统一 RPC channel+DTO+路由。
- [JD预测] Web 端上线了吗？→ 真话：桌面为主，Web 端是验证「同一套逻辑能否复用」的第二个载体；重点不是两个产品，是传输层只写一遍。
- [JD预测] 文件系统两端怎么处理？→ 桌面本地直连、Web 经服务端代理，差异收敛在 DTO 与路由层，上层无感。

**R24 组（v3 降级：背景能力 · 非五点）**
> 注（B 问题5）：R24 已移出选点，本组保留作背景能力——被问「项目工程保障」时按此答，不把 R24 当第五个点讲。
- [背景] 项目工程上怎么保证不写崩？→ 类型检查（可当场演示 tsc）+ 每机制自带测试（路由穷举/事件 parity 等）——确定性验证底座；契约层加固（Channel Map parity 等）是可选机器，未全量上。
- [背景] 研究结论质量怎么评估？→ 工程层=确定性验证（事件可回放/回归可定位）；行为评测（任务成功率/轨迹回放/LLM 裁判）是方法论认知，项目未实现——诚实交底。

### Part 3 · 弱点追问与降级路径

| # | 弱点追问 | 严重度 | 防线 | 降级路径 |
| --- | --- | --- | --- | --- |
| 1 | Pi 具体怎么接的？流式返回什么结构？（三层追问链） | 高 | §5-1：PiAgent=进程外 JSONL 子进程（init/prompt/abort 消息），工具调用代理回宿主；事件=可辨识联合五类 | 协议细节接不住 → 退「理解设计层」：核心是 stdin/stdout JSONL + 工具代理；再退 → 转讲 R12/R10 工程层，承认后端接入细节以抽象层掌握为主 |
| 2 | MCP 协议下沉（JSON-RPC/握手/tools 映射字段） | 高 | §5-2 先发自爆，抢在被问前 | R18→R17：「核心贡献是工具上下文抽象（sessionId/workspace/文件系统/凭据/回调统一注入），MCP 只是其中一种 Source 类型」 |
| 3 | 多任务并行是伪需求/就是多开窗口 | 中 | §5-4：隔离工具上下文/目录/偏好，非 UI 窗口 | 接不住 → 退「至少解决了单任务内搜索结果/原文/结论混杂」的上下文管理价值 |
| 4 | 个人项目为什么这么重的工程体系 | 中 | §5-6 单人防御叙事（v3 重锚事件契约+路由边界）+ 主语换「可复用工程底座，研究是第一个垂直场景」 | 无需降级，这是叙事牌不是技术题 |
| 5 | Web 端做了吗/部署了吗 | 中 | §5-5 真话口径 | 无需降级 |
| 6 | 事件流是不是就是 async generator 换皮（R10 升格） | 高 | §5-10 四件套：EventQueue（push→pull 桥接+缓冲+完成信号+reset）、turnId/工具树关联、typed_error 恢复语义、双后端适配层——「换皮只需要 yield」 | push/pull 概念接不住 → 退工程动机「两个后端 SDK 消费方式不同，EventQueue 把回调变成可迭代」；再退 → 「至少统一了文本/工具/错误三类的事件形状」 |
| 7 | 研究质量怎么评估/有没有数据 | 中 | R24 组[背景]口径 + 事件流是行为可复现的数据基础（非评测本身） | 红线：不编数据；退「定性目标」话术 |
| 8 | 简历为什么没写 i18n（若被问项目里有没有） | 低 | 不主动提；被问则答「项目有 i18n 资源完整性校验，与研究场景无关故未写进简历」 | 无需降级 |
| 9 | 事件到底几类？五类就这？（R10 v3 新增） | 中 | §5-10 五类口径：类别口径五类+收尾态，底层联合类型还有辅助事件按需扩展（类别+用途，不背全表） | 被追具体字段 → 锁死五类+turnId 关联+typed_error 五字段一个真实例子 |
| 10 | 事件流都能回放了，怎么不做评测？（R10 v3 新增） | 中 | §5-12：评测管线=采样+判定+裁判三独立问题，事件流是数据基础非评测；不越 Scope | 诚实交底「没实现就说没实现」——红线兜底，不技术硬扛 |
| 11 | 嵌套工具树 Pi 后端怎么表示？（R10 v3 新增） | 中 | §5-10 诚实限定：Claude 原生透传 parentToolUseId，Pi 用 sub-turn 隔离近似，UI 兼容可选字段 | 退「turnId 保证一轮归组，嵌套关联讲 Claude 路径是扎实的」——主动承认 Pi 弱项好过被戳穿 |

> 步5 完成（v3 重跑）。Craft 五步链 v3 收口。步6 联动/步7 简历随全局换点同步。
