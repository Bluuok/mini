# HappyClaw / Clawtide 当前定稿入口

> 2026-09-19 复审。实现快照：`Bluuok/Clawtide@dde300313de851baa953f24f6a0f8c75e9cacd3f`。项目定位：个人自托管 Agent 应用；跨境电商是演示场景，不是商家接入或生产部署证明。

## 1. 简历正文

# Clawtide · 自托管数字员工工作站

基于 HappyClaw 参考架构复现并实现的个人项目，以跨境电商运营为演示场景，将 IM 消息与定时任务接入 Agent 执行链，并提供 Profile 配置、工作区授权和 Web 管理入口。

**技术栈：** TypeScript、Node.js、Hono、SQLite、React、WebSocket、Claude Agent SDK。

- **任务物化与条件认领** 针对定时任务重复触发与执行记录难以追踪的问题，基于 SQLite 唯一键和条件更新，设计了 occurrence 物化、owner/token 认领及终态记录链路，实现了发生点去重与任务状态更新的条件校验。

- **Agent Profile 与配置变更** 针对数字员工规则散落、局部修改易覆盖其他配置的问题，基于四段式 Prompt 与版本快照，设计了独立字段更新、历史内容恢复和草稿短语确认入口，实现了配置分段维护与历史可回看。

- **IM 适配与会话接入** 针对不同 IM 消息格式和会话形态难以复用的问题，基于 ImChannelAdapter 与能力声明，设计了飞书、Telegram 适配及消息规范化链路，实现了渠道消息经挂载解析进入统一执行入口，其余渠道保留为骨架。

- **工作区授权与 IM 准入** 针对管理角色与资源归属混用的问题，基于所有者判定和发送者身份校验，设计了读、改、删独立授权函数与 audience_mode 准入分支，实现了管理员无所有权旁路、Home 禁删及拒绝分支不执行。

- **会话认证与登录限流** 针对 Web 管理入口的身份冒用与重复登录尝试，基于随机会话 token、HMAC 签名和进程内计数，设计了签名核验后查库、双名 Cookie 及两层限流，实现了失效会话拒绝与账号维度的尝试约束。

## 2. 场景落点

最小演示是整理用户手工提供的运营事项文本，输出待确认事项和建议；定时任务可以把同一段静态文本放入 task.prompt 再处理。没有新的数据源 Adapter，就不能将定时运行解释为获取当天库存、自动巡检实时订单或动态日报。结果在控制台留存，普通 IM 回复与调度 notification 日志是不同路径。

## 3. 保持五点，但不混合保证

调度管发生点与数据库状态；Profile 管声明配置及其编辑；IM 管渠道格式和会话定位；授权管资源动作；认证管 Web 用户身份。三个入口都到 AgentRuntime，不证明 Profile 与授权参数相同。定时入口未装配 Profile；失租尚未传播为执行器取消；这些是业务实现缺口，不因文案收窄而算作修复。

## 4. 证据与阅读顺序

- [故事主线](HappyClaw-故事.md)与[完整八模块准备稿](修改/HappyClaw/HappyClaw-故事.md)。
- [十五题 QA](修改/HappyClaw/HappyClaw-面试QA.md)、[源码导航](修改/HappyClaw/evidence/源码导航.md)。
- [原有待补验证清单](修改/HappyClaw/HappyClaw-源码口径与待补验证.md)及[本轮技能对抗评审与额外逻辑终审](修改/复审-2026-09-19/README.md)。

## 5. 不能扩大成的能力

两个 SDK 适配不等于七渠道已接通；everyone 普通消息可进入后续授权，不是默认 Owner-only。三个动作检查都返回 allow/deny，并非三个返回状态。短语确认只用于草稿发布，不覆盖普通 CRUD；Prompt 固定前导块和 PostToolUse 留痕不是工具执行前强制权限。Cookie、HMAC、资源授权与进程内限流各自有边界，不等于运行时沙箱或分布式安全。

源码支持当前模块机制；本人已确认围绕五点复现并实现，真实模型/IM 联调与业务效果仍分别记录。本文件不认领整个上游或 SDK 的开发，也不新增性能数字。
