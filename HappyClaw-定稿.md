# HappyClaw 定稿工作文件（步4 修订版 · 五步链已收口 · 选点 v2：R25→R19）

> 项目：HappyClaw（个人项目）｜场景：跨境电商 AI 数字员工｜选点 R07 R14 R15 R19 R20
> 选点变更记录（2026-08-24）：原选点 R07 R14 R15 R20 R25，**R25（行为评测底座）→ R19（认证与会话管理）**。理由：R25 依赖未选点 R06/R24、评测方法论易被追到 L3 兜不住；R19 是 Medium、Scope 清晰（Web 用户认证）、好防守，且与 R20 构成「认证 vs 授权」安全双支柱。
> 评测思想保留（用户指示）：**不单独成选点**，只保留两个很轻的动作——轻量业务 Review + 典型 Case 回归。详见 §2.5 思想④ 与 §5-13。
> 红线：验证效果全定性；不虚构使用主体/业务反馈；未实现不写成已实现；论文数字只进 talking 严禁进 bullet。
> ⚠️ 编号铁律：HappyClaw 的 Rxx 与 MiniCode/Craft 各自独立，**绝不能跨项目套用编号语义**。本文件内 R07=7渠道IM抽象、R14=定时调度、R15=Agent Profile、R19=认证与会话管理、R20=RBAC三态+工作区所有权。

---

## 0. 四字段（步4 修订版）

- **项目名称**：HappyClaw
- **一句话简介**：面向跨境电商场景的自托管 AI 数字员工平台，把数字员工的人设治理、IM 与定时双入口值守、认证授权与权限保障做成可控、可运行、可信赖的 AI 应用。
- **技术栈**：TypeScript、Node.js、Hono、SQLite、React、Docker、WebSocket、Claude Agent SDK
- **项目定位**：个人项目 · 面向多用户的自托管数字员工工作站 / AI 应用平台（AI+业务闭环，非 Agent Infra；跨境电商为演示垂直场景）

> 步4 修订记录（A/C 裁决）+ 选点 v2：
> ① 简介与定位加「自托管」锚定——「多用户」必须被读成「平台支持多用户的产品形态（RBAC 支撑）」而非「很多用户在用的规模声明」（A②/D③）；
> ② 定位字段显式带「跨境电商为演示垂直场景」头罩，罩住所有 bullet 的场景主语（A① 修法 b）；
> ③ 删「无 RAG/知识库」否定式自曝（C2：智能客服岗 RAG 是核心预期）——缺口移入防线 §5-9 交底；
> ④ 双名词收敛为「数字员工工作站 / AI 应用平台」；
> ⑤ **选点 v2**：简介里「行为质检 / 可评测」→「认证授权与权限保障」（R25 移除，评测不进简介）。

## 1. 步1 基线存档（逐字引用 resume-points.md，供对照修订；面试口径一律以 §3 改写版为准）

### R07 基线（Medium）
针对企业多渠道接入时 Agent 核心耦合渠道 SDK、扩展新渠道成本高的问题，基于适配器模式，设计了 IMChannel 统一接口（飞书、Telegram、QQ、钉钉、微信、Discord、WhatsApp 七渠道统一抽象），实现了 Agent 核心不感知渠道差异。
- Scope：微信=iLink/仅P2P 非企业微信；企微走 cc-connect wecom；各渠道能力有差异，**非"功能完全一致"**。Q07.x｜W04 W07

### R14 基线（Medium-High · 超档）
针对多实例部署下定时任务重复执行与并发竞争的问题，基于租约互斥与不可重入调度，设计了 V2 Occurrence Materialization 定时任务调度器（cron 最小 60s/interval/once 三类型、SQLite 租约互斥 lease_owner/lease_token、循环 pumpTaskScheduler），实现了多实例安全调度。
- Scope：未单列额外边界；核心实体=materialization 先落记录再执行+SQLite 租约互斥。Q14.x｜W11

### R15 基线（Medium）
针对数字员工人设缺乏结构化约束、修改无安全确认导致行为失控的问题，基于四段正交 Prompt 工程，设计了 Agent Profile 四段式 Prompt（IDENTITY/SOUL/AGENTS/TOOLS、append/replace 双模式、两阶段 AI 辅助与确认短语安全流程），实现了人设管理的可控与一致。
- Scope：AGENTS 段=声明性 prompt 非可执行编排引擎；平台无 RAG/向量检索/知识库。Q15.x｜W12

### R19 基线（Medium · v2 新增）
针对会话 Cookie 可伪造、登录接口可被爆破的问题，基于 HMAC 签名与双层限流，设计了 Cookie Session 认证体系（30 天、64 字符 hex token、HMAC-SHA256 签名、常量时间比较防时序攻击、__Host- 前缀 Secure Cookie、per-IP + per-username 双层限流），实现了认证与登录安全加固。
- **⚠️ Scope 边界（关键）**：**Web 接入层用户认证**，非 Agent 运行时身份（运行时身份走 R01/R23）；**IM 侧命令授权走 R20 IM Owner Gate，非 R19**。即：R19=登录认证（你是谁）、R20=授权（你能做什么），**认证与授权分属两层，不得混谈**。Q19.x｜W09

### R20 基线（Medium-High · 超档）
针对多租户场景下越权访问与管理员权限过大的问题，基于 RBAC 三态模型与渠道身份匹配，设计了权限体系（canAccessGroup/canModifyGroup/canDeleteGroup 三态、admin 不自动 bypass 工作区所有权、IM Owner Gate 渠道身份匹配），实现了企业多租户隔离。
- ⚠️ 基线效果槽「企业多租户隔离」与其自身 Scope「非 R23 多租户隔离」矛盾（步1 发现，步3 已删）；基线的「企业多租户」措辞在个人项目下是虚构主体雷区——**面试勿复述基线原文，一律用 §3 改写版**。Scope：属工作区所有权/授权矩阵，非 R19 Web 认证、非 R23 运行时隔离。Q20.x｜W09 W21

## 2. 场景包（步2 · 强模型档产出 · v2 微调）

> **世界观声明（全链最高红线）**：HappyClaw = 个人开发的自托管多用户数字员工平台；「跨境电商 AI 数字员工」（客服/运营/售后）是其**垂直演示场景**。使用者 = 本人部署自用（平台形态上任何人可自托管）。下方公司画像与 JD 为**求职目标画像**（我要投的公司/岗位），不是"本项目服务过的公司"；所有痛点为该类业务通用痛点，全部标【合理假设】，不编精确数字，不虚构使用主体与业务反馈。（Craft 步4 教训：个人披企业皮 = 系统性死因，此处从根上规避。）

### 2.1 目标公司画像【合理假设】
- **行业**：跨境电商（出口方向，多平台店铺：亚马逊 / TikTok Shop / Shopee / 独立站）
- **业务模式**：多店铺 × 多市场运营，客户咨询跨时区、跨语言；日常运营动作（巡检 / 提醒 / 播报）高频重复
- **规模**：中小跨境团队（数十人），技术团队精简，AI 应用岗少量编制——要一人扛"AI 应用从接入到质检"全链
- **协作方式**：IM 为工作中枢（内部飞书/钉钉为主，对客触达 WhatsApp/Telegram 等）+ 管理后台
- **技术栈偏好**：TypeScript/Node.js 服务端、React 前端、SQLite 级存储、云上自托管部署、LLM 应用层（Agent/工具调用）
- **用的 IM**：内部协同飞书/钉钉；客户侧 WhatsApp/Telegram 等——正好对应 R07 七渠道的真实分布

### 2.2 目标岗位 JD（模拟）【合理假设】
**岗位：AI 应用开发工程师（跨境电商数字员工方向）**
职责：
1. 设计开发基于 LLM 的数字员工/智能客服应用：IM 渠道接入、会话状态、工具调用链路
2. 打通 Agent 与业务集成：业务能力工具化、定时任务主动触发、多渠道消息路由
3. 建立数字员工的人设与权限治理：结构化 Prompt 管理、RBAC 授权、变更安全流程、登录安全
4. 建立 Agent 行为的质量保障：变更验证、回归、效果度量

任职要求：TS/Node.js 扎实；React；理解 Agent 执行循环与工具调用；有工程质量意识者优先。
**关键词**：数字员工 · IM 集成 · 定时任务 · Prompt 工程 · 权限治理 · 登录与认证安全 · Agent 工具调用

### 2.3 业务痛点 → 选点映射（每条【合理假设】）
| # | 痛点 | 对应 R |
| --- | --- | --- |
| P1 | 客户咨询散落多渠道，逐渠道对接 SDK，Agent 核心被渠道细节绑架，加一个渠道改一遍代码 | R07 |
| P2 | 重复性运营动作（异常巡检/库存汇率提醒/日报播报）靠人工按时执行，跨时区易漏做迟做；多实例部署时定时任务重复触发与竞争 | R14 |
| P3 | 机器人人设靠散写 prompt，缺结构化约束，改一句行为跑偏一片，修改无安全确认 | R15 |
| P4 | 数字员工管理后台是配置渠道、人设、权限的入口——登录 Cookie 可伪造、登录接口可被爆破，后台可能被未授权进入 | R19 |
| P5 | 多角色共用工作区时归属不清、读/改/删无分级规则，admin 权限过大无制衡；IM 里谁说话数字员工都当老板 | R20 |

### 2.4 关键词 → 热点映射
- 登录/认证/会话安全/防爆破 → **R19**（v2 新增；Scope：Web 接入层用户认证）
- 安全/合规/权限/审计 → **R20 兜底**（R27 未选；防线：「安全纵深是演进方向，我实现的是授权矩阵 + 登录认证这一层」）
- 评测/质检/可观测 → **不单独成选点**：只保留轻量业务 Review + 典型 Case 回归（§5-13），被深挖评测方法论则承认是前沿认知未实现
- 技能沉淀/进化 → R26 未选 → 不主动展开；被问走 R15 口径（生成-审批-发布安全闭环）
- IM 集成 → R07；定时调度 → R14
- 工具调用/会话状态 → 半缺口，走防线 §5-8（SDK 托管执行循环+策略治理与留痕两层）
- ⚠️ JD 若出现 RAG/知识库：HappyClaw 无此能力（Scope 明确），防线 §5-9 交底取舍逻辑

### 2.5 用户画像五思想采纳记录
| 思想 | 处理 |
| --- | --- |
| ① Agent=数字员工，Profile 是核心 | 锚入 R15（bullet 主位）+ 四字段定位 |
| ② 消息+定时双入口 | 锚入 R07（被动入口）+ R14（主动入口），bullet 相邻排列 |
| ③ 多渠道只是统一入口，渠道与 Agent 解耦 | 锚入 R07；重点不在 IM 技术本身，在解耦 |
| ④ Evaluation 质检闭环（执行→评审→修改→回归） | **v2 降级**：不单独成选点（R25 已移除），只保留轻量业务 Review + 典型 Case 回归两个轻动作（§5-13）；「评审→修改自动闭环」属 LLM 裁判方法论、未实现，只作口头演进方向。与前两个项目的评测差异不再主打 |
| ⑤ AI+业务闭环而非 Agent Infra | 锚入定位字段（AI 应用平台，非 Infra） |

## 3. 对齐改写（步3 · 强模型档产出 · 步4 修订后定稿 · 选点 v2）

### 3.0 排序（v2：安全双支柱收尾）
R07（被动入口）→ R14（主动入口）→ R15（数字员工核心）→ R19（登录认证：你是谁）→ R20（授权：你能做什么）。理由：沿画像主流程「双入口 → Agent → 治理」，末两项是「认证→授权」的安全双支柱（R19 先证明身份、R20 再判权限），R20 较重收尾。

### 3.1 五条定稿 bullet（问题槽=场景痛点；方案槽守住基线技术语义；效果槽定性）

#### R07 · 多渠道统一入口（Medium）
针对跨境电商客户咨询散落在飞书、WhatsApp、Telegram 等多个 IM 渠道、逐渠道对接 SDK 使 Agent 核心耦合渠道差异、每扩展一个渠道都要改造一遍接入代码的问题，基于适配器模式，设计了 IMChannel 统一接口（七渠道统一抽象与统一收发接口），实现了 Agent 核心不感知渠道差异、新渠道接入收敛为适配器级改动。

> 步4 修订记录：「收发路径」→「收发接口」（B5：消掉「路径」的路由歧义，防蹭未选点 R08 JID 寻址）。问题槽主语由 §0 定位字段「演示垂直场景」头罩罩住（A① 修法 b）。

#### R14 · 定时主动入口（Medium-High · 超档保留）
针对跨境电商日常运营中异常巡检、库存与汇率提醒等重复性动作依赖人工按时执行、跨时区易漏做迟做，以及自托管部署多副本时定时任务重复执行与并发竞争的问题，基于租约互斥与不可重入调度，设计了 Occurrence Materialization 定时任务调度器（cron 最小 60s/interval/once 三类型、SQLite 租约互斥 lease_owner/lease_token、循环 pump 调度驱动），实现了数字员工定时值守的安全调度与多实例互斥。

> 步4 修订记录（B1/D⑤）：问题槽「平台支撑多工作区并行时」→「自托管部署多副本时」——租约互斥解决的是**多实例（进程级）竞争**，「多工作区」（数据层概念）与之错位，回归基线口径；效果槽「多实例互斥」保留，问题-机制对齐。删「V2」（D⑤：纯邀请「V1 是什么」追问零收益）；pumpTaskScheduler→「循环 pump 调度驱动」（D 泛化档）。「多实例」答问口径见 §5-2，lease HOW 见 §5-6。

#### R15 · 数字员工核心（Medium）
针对数字员工人设依赖散装 Prompt、缺乏结构化约束、修改无安全确认导致单次改动引发行为失控的问题，基于四段正交 Prompt 工程，设计了 Agent Profile 四段式 Prompt（IDENTITY/SOUL/AGENTS/TOOLS、append/replace 双模式、两阶段 AI 辅助与确认短语安全流程），实现了数字员工人设的可控管理与一致行为。

> 步4 修订记录：无实质修改（A 判定本条个人单用户动机成立——确认短语防的是「对话里顺手改坏人设」，不依赖多用户）。

#### R19 · 后台登录认证（Medium · v2 新增）
针对跨境电商数字员工平台管理后台（配置渠道、人设与权限的入口）的登录会话 Cookie 可伪造、登录接口可被爆破导致后台被未认证进入的问题，基于 HMAC 签名与双层限流，设计了 Cookie Session 认证体系（服务端生成随机 token 存会话表、HMAC 签名作防篡改兜底、常量时间比较防时序攻击、__Host- 前缀 Secure Cookie、per-IP + per-username 双层限流），实现了后台登录的认证加固与接口爆破的限流防护。

> v2 新增加注：Scope=**Web 接入层用户认证**（后台登录），**非 Agent 运行时身份**；IM 侧「谁能触发数字员工」走 R20 IM Owner Gate，**R19 与 R20 分属认证/授权两层**，不得混谈。问题槽「未授权进入」→「未认证进入」（B 审查：从 R19 auth 句剔除「授权」字眼，防与 R20 词域污染）。Q19.x｜W09。

#### R20 · 权限治理（Medium-High · 超档保留 · 已收窄）
针对面向多角色共用工作区的授权治理中访问/修改/删除权限无分级规则、管理员权限过大缺乏制衡导致越权操作难防范的问题，基于 RBAC 三态模型与渠道身份匹配，设计了权限体系（canAccessGroup/canModifyGroup/canDeleteGroup 三态分离、admin 不自动 bypass 工作区所有权、IM Owner Gate 渠道身份比对 owner_im_id），实现了读/改/删分级管控可落地与工作区数据归属清晰。

> 步4 修订记录：
> ① 问题槽「多人多角色共用数字员工平台时」→「面向多角色共用工作区的授权治理中」（A④：把「多人共用已发生」的事实感软化为设计目标措辞；D③ 同向）；
> ② 效果槽「工作区数据归属清晰与越权访问风险的收敛」→「读/改/删分级管控可落地与工作区数据归属清晰」（B2：「越权…风险收敛」逐字撞 R23 标志句型，用 R20 授权矩阵专属话术替代）；
> ③ 「多用户」定位支撑话术见 §5-3（D③：R20 是唯一扛「多用户」的点，必须配改口防线）。

### 3.2 Talking points（四段式 · 步4 修订版 · v2）

**R07**
- 问题背景：跨境客户在 WhatsApp/Telegram/飞书等渠道都有咨询，逐渠道接 SDK 会把消息格式、附件、会话模型的差异全部灌进 Agent 核心，加渠道=伤筋动骨。
- 实现方案：IMChannel 统一接口+各渠道适配器，收发接口统一，Agent 核心只面向统一消息模型。
- 设计权衡：抽象抹平差异也意味着放弃部分原生特性——各渠道能力本就不齐（微信 iLink 仅 P2P、WhatsApp 有风控），按公共能力设计接口，渠道特有能力在适配器层显式暴露而非假装一致。
- 局限扩展：企业微信/Slack 等未覆盖渠道走外置桥接（cc-connect），非原生；这是接入层解耦，不含消息寻址路由的深水区（JID 寻址为未选点，知道边界即可）。

**R14**
- 问题背景：数字员工的价值在「主动值守」——定时巡检/提醒没人盯着点就会漏；自托管部署为可用性起多副本后，同一任务就会被重复执行。
- 实现方案：先把任务物化为 occurrence 记录再执行；SQLite 租约（lease_owner/lease_token）互斥保证同轮次仅一个实例执行；循环 pump 驱动；心跳续租延长租期防止没跑完被抢。
- 设计权衡：用 SQLite 租约而非分布式锁——自托管场景部署面小，DB 租约足够且可审计；不可重入设计宁可错过不可重复。占锁靠条件更新+租期判断判赢家（UPDATE 带 WHERE lease_expires_at 判断，受影响行数定胜负），lease_token 是认领凭据：续租/交接凭它校验归属，防过期实例拿旧 token 污染状态。
- 局限扩展：最小粒度 60s——粒度决定 DB 写频率，且巡检/播报类主动场景 60s 足够（备答「为何不做 1s」）；长任务撞自身间隔时按不可重入语义跳过本轮，宁可错过不并发重跑；调度器解决「什么时候做什么」，不解决「做得好不好」（质量验证走 §5-13 轻量 Review/回归）。

**R15**
- 问题背景：数字员工的核心资产是人设与工作规则；散装 prompt 一改就跑偏，且改动即生效、无安全网。
- 实现方案：IDENTITY/SOUL/AGENTS/TOOLS 四段正交（我是谁/价值观底线/工作规则/工具策略）独立配置互不污染；replace 模式保证平台运行时指令不可清除；两阶段 AI 辅助起草+确认短语发布。
- 设计权衡：AGENTS 段是声明性工作规则而非可执行编排引擎——可控性来自结构化+审批流，不是假装能编程；确认短语是跨轮安全令牌，防止对话里顺手改坏人设（单用户也成立：Agent 不应能未经确认不可逆地修改自己的核心资产）。
- 局限扩展：无 RAG/知识库，知识靠上下文窗口+工具调用；append 模式适合增量补充规则但会累积冗余，replace 干净但易误删——双模式是给不同改动类型的安全档位；技能自动进化未实现（演进方向，不展开）。

**R19**
- 问题背景：管理后台是配置渠道、人设、权限的入口，登录一旦失守等于把整个数字员工后台交给攻击者；Web 登录最常栽在两处——会话 Cookie 可伪造、登录接口可爆破。
- 实现方案：**单条自洽链**——服务端生成随机 token 存会话表 → 对 token 做 HMAC 签名作防篡改兜底 → 会话查找用常量时间比较防时序侧信道 → __Host- 前缀 Secure Cookie 携带 → per-IP + per-username 双层限流（读/写路径）。对称密钥走环境变量注入，不进代码、不落盘；因为 token 有服务端会话表，轮换密钥只需换 key + 清会话表全体重登，是一次干净操作。
- 设计权衡：真 opaque 的随机会话 id 与 HMAC 签名各司其职——随机 id 不可预测、服务端可即时吊销（有会话表），HMAC 是防篡改兜底（防有人在 cookie 里拼一个不存在的 id 撞会话查找）。常量时间比较规避「逐字符比对早失败」的时序攻击；双层限流兼顾「单 IP 扫号」与「单号爆破」两种形态。但这是**登录认证**这道闸——IM 侧「谁能触发数字员工」走 R20 IM Owner Gate，认证与授权分属两层。
- 局限扩展：这是 **Web 接入层用户认证**，非 Agent 运行时身份（运行时身份走 R01/R23，未选点）；基础 Cookie 属性（HttpOnly / Secure / SameSite=Strict）已带；**CSRF token 与登录后重生成 session 这些 Web 安全第二层是演进方向**，未上（个人自托管后台无并发、无敏感资金操作）；与 R20 分界是「认证 vs 授权」，别混成安全大而全。

**R20**
- 问题背景：平台面向多用户设计——多角色共用工作区时「谁能看/改/删」必须分级；若 admin 天然绕过一切，工作区所有者的数据归属就是空话。
- 实现方案：三态函数组（access/modify/delete 分离判定）+ admin 不自动 bypass 工作区所有权 + IM Owner Gate（渠道原生发送者 ID 比对 owner_im_id）。
- 设计权衡：分权模型而非超级管理员模型——牺牲 admin 便利换取归属确定性；IM 侧授权不走 Web 登录身份（那是 R19）而走渠道身份匹配，因为触发主体是 IM 消息。admin 不 bypass 的配套是受控的所有权转移路径（恢复场景走 reassign owner，而非天然旁路）——分权不是造出没人管的黑洞。
- 局限扩展：本层职责边界是授权矩阵（判定谁能做什么）；更深层运行期隔离作为演进方向（自托管单机场景下进程级隔离够用，多租户硬隔离是平台化后的课题）。IM Owner Gate 判定链：消息到达→解析渠道原生发送者 ID→与工作区 owner_im_id 比对→不匹配则拒执行该消息（不回执，防探测）→owner_im_id 在工作区创建/绑定渠道账号时写入。

### 3.3 场景映射表
| Bullet | 问题槽来源 | 效果槽处理 | Scope 检查 |
| --- | --- | --- | --- |
| R07 | ← P1 | 定性追加「适配器级收敛」 | ✓ 无越界 |
| R14 | ← P2 | 合并「值守+互斥」定性 | ✓ 技术核心保基线 |
| R15 | ← P3 | 保基线定性 | ✓ AGENTS≠编排引擎已守 |
| R19 | ← P4 | 保基线定性「认证加固+限流防护」 | ✓ Web 用户认证内，不碰运行时身份/IM 授权 |
| R20 | ← P5 | 收窄（删多租户隔离+授权矩阵专属话术） | ✓ 授权矩阵内 |

## 4. 审查记录

### 第一轮（选点 v1 · R25 版）
四审查员并行：A 叙事 60→75 / B 归属 84→90+ / C 覆盖 78 / D 终审 45→75。裁决采纳：删 V2、R14 问题槽「多工作区」→「自托管多副本」、R20 效果槽删「多租户隔离」+换授权矩阵话术、定位字段删「无 RAG」自曝、R25 bullet 去计数与组件名、pass^k 不主动提。
> ⚠️ 该轮评审对象已含 R25；部分结论（R20/R14/定位/零共享）在 v2 仍有效。

### 第二轮（选点 v2 · R25→R19 + 评测降级 · 2026-08-24 · 再审）
- **变更动因**：R25 依赖未选点 R06（StreamEvent）/R24，评测方法论（LLM 裁判/pass^k）易被追到 L3 兜不住；R19 更稳（Medium、Scope 清晰、好防守），且与 R20 成「认证 vs 授权」双支柱。评测思想按用户指示降为轻量业务 Review + 典型 Case 回归，不进选点。
- **B 归属 93/100**：R19 Scope 精确限 Web 用户认证、认证/授权分工清晰、组件名全源真实、评测降级干净。仅两处低危微词：「未授权进入」→ 已改「未认证进入」（R19 auth 句剔除「授权」字眼）；§5-8「行为可归因」→ 改「副作用可追溯」（避 R25 句型）。
- **D 终审 72/100（可到 86）**：新点 R19 自伤性雷最多——①HMAC 签名 vs opaque 随机 token 自相矛盾（已补 §5-12 单条自洽链 + key 管理）；②Web 安全连招 CSRF/session fixation 零设防（已补 §5-13 第二层演进方向边界）；③分布式限流绕过（补入 Q&A R19 组 + Part3 #7）；④R20 对象级授权缺口（补 Part3 #8）；⑤R14 验证裸奔（R25 兜底抽走，补 Part3 #13 + §5-6）。R19 严重度由「中」上调为「高」（Part3 #5/#6）。

**v2 采纳清单**：R19 问题槽「未授权」→「未认证」；§5-8「行为可归因」→「副作用可追溯」；新增 §5-12（R19 token 自洽链+key 管理）、§5-13（Web 安全第二层演进方向）、§5-14（轻量评测声明）；Part3 表扩到 15 条（R19 内部矛盾上调为高、新增 CSRF/分布限流/对象级/R14 验证四条）；Q&A R19 组补「session fixation/CSRF/分布式限流」追问；§3.2 R19 talking 改单条自洽链叙事。

## 5. 面试防线口径（背熟 · v2）

1. **世界观头罩（全场第一句）**：这是我个人开发的自托管数字员工平台——平台支持多用户多角色的产品形态，跨境电商客服/运营是我给自己配的演示垂直场景。任何被问「谁在用」都先回到这句。
2. **多实例口径（R14）**：「多实例是自托管部署的通用可用性需求——用户为可用性起多副本是真场景。我按这个通用形态设计了 SQLite 租约互斥并验证。」绝不说「我自己开了很多实例玩」。
3. **多用户话术（定位支点，D③）**：「多用户是设计目标：我实现的是单实例可运行的多角色授权与所有权模型，实际验证到工作区/角色级（测试用例模拟多角色越权）；运行时多租户硬隔离是平台化后的演进方向。」把「已成立事实」降为「设计目标+可自托管形态」，诚实不掉价。
4. **IM Owner Gate 判定链（D① 最重零防线雷）**：消息到达→解析渠道原生发送者 ID→与工作区 owner_im_id 比对→不匹配则拒执行该消息且不回执（防探测）；owner_im_id 在工作区绑定渠道账号时写入。被追跨渠道身份：「同一人跨渠道按渠道账号各自绑定，不做跨渠道身份归一——那是身份系统的课题。」
5. **admin 孤儿回收（D④）**：「分权的配套是受控的所有权转移路径（reassign owner），恢复场景走受控转移而非天然旁路。转移机制的完整实现是演进方向，当前实现的是准入级三态判断+初始 owner 绑定。」
6. **lease 原子占锁 HOW 层（D②）**：占锁=条件更新+租期判断（UPDATE 带 WHERE lease_expires_at 过期条件，受影响行数定赢家，SQLite 单写者保证原子）；lease_token=认领凭据，续租/交接凭它校验归属，防过期实例拿旧 token 写状态；心跳续租延长 lease_expires_at 防「没跑完被抢」。被追到极端时钟场景：「我讲得清设计原理，并发用例在测试套件里验证过；极端时钟漂移压测没做。」
7. **materialization 动机（删 V2 后的备答）**：「直接执行模式下到期补跑（backfill）和并发去重都要靠运行时猜；物化先把『该跑的一次』落成记录，执行只是消费记录——backfill 变成扫表，互斥变成对记录抢租约。V1 到这一版的演进就是这个思路转变。」
8. **工具调用/会话状态缺口防线（C1，JD 原话）**：「执行循环与会话由 Claude Agent SDK 托管，我在其上做两层：数字员工工具策略的声明式治理（Profile TOOLS 段：可用工具/用法边界）+ 工具调用全链路留痕（事件轨迹记每次调用的入参/出参，副作用可追溯）。」
9. **RAG 交底话术（C2）**：「客服场景的确定性数据（订单/库存/政策）我用工具调用读 DB/API——有证据链、结论可追溯，不交给模型幻觉；语义模糊的开放问答才是向量检索的主场，那是我理解的演进方向（分块/embedding/检索增强），未实现。取舍核心是我重视结论可追溯。」
10. **React 悬空防线（C3）**：「前端重心是实时状态而非样式：管理后台的消息流虚拟化、流式输出渲染、断线指数退避重连。」（评测可视化已随 R25 移除，不再提轨迹回放视图。）
11. **认证 vs 授权分界（R19/R20，v2 新增防线）**：「R19 是 Web 后台登录认证——证明你是谁；R20 是授权——决定你能做什么，含 IM 侧命令授权（IM Owner Gate）。两者分属两层，合起来才是完整的访问控制。别把 R19 说成『运行时身份』。」
12. **R19 token 自洽链 + 密钥管理（v2 新增 · D 判最致命雷）**：单条链讲清「服务端生成随机 token 存会话表 → HMAC 签名作防篡改兜底 → 常量时间比较查会话 → __Host- Cookie 携带 → 双层限流」；密钥走环境变量、不进代码不落盘，轮换=换 key+清会话表（干净操作）。**别把 HMAC 签名和 opaque 随机会话 id 当两个并列方案讲**——一个是防篡改兜底、一个防不可预测+可吊销，各司其职。被追问「opaque 为什么还要 HMAC」用这句化解。
13. **Web 安全第二层是演进方向（v2 新增 · D 判连招防御 · 2026-08-25 按源码核实修订）**：「基础 Cookie 属性我已带齐——HttpOnly、Secure、SameSite=Strict；我先解决『你是谁』的可信身份（登录认证），这一层做完；CSRF token 与登录后重生成 session 属于我认知里 Web 安全的第二层，是演进方向——个人自托管后台无并发、无敏感资金操作，所以没上。」把「没做」明说成边界，不说成忘了；SameSite 由『未上』改『已有』是与参考源码核实后的对齐（auth.ts:86），别说反。
14. **轻量评测声明（用户指示 · v2 替代 R25 位）**：「评测我不做成完整底座，只保留两个轻动作——典型 Case 回归（改动人设/工具后跑几个代表用例对齐预期）+ 轻量业务 Review（上线前人工过一遍关键业务流程）。被深挖评测方法论（LLM 裁判/pass^k），我承认那是前沿认知，项目未实现、无实测数据。」论文数字永远不进 bullet。

## 6. 源码补课清单（带读 · v2 更新）

1. **R20 RBAC + IM Owner Gate（最高优先，D 判零防线雷最多）**：canAccessGroup/canModifyGroup/canDeleteGroup 三函数的判定输入与组合关系；IM Owner Gate 比对失败的 deny 分支真实实现（回执还是静默？）；owner_im_id 的写入时机与数据来源；admin 权限的真实边界（有没有 reassign/transfer 实现）。
2. **R14 租约调度 HOW 层（第二优先）**：lease_owner/lease_token/lease_expires_at 三字段写入与校验路径；占锁 SQL 条件更新原句；心跳续租实现（间隔=租期/3 出处）；pump 循环重入保护与空轮行为；materialization 记录生命周期。
3. **R19 认证与会话管理（v2 新增 · Medium 可攻）**：Cookie session token 的生成与 HMAC 签名实现；常量时间比较的具体函数；双层限流的计数与触发阈值；会话过期/续期逻辑。对应 W09。
4. **R15 Profile 结构**：四段在数据模型里的存储形态；replace 模式「平台运行时指令不可清除」的具体实现；确认短语的生成与校验流程。
5. **R07 渠道适配器**：IMChannel 接口的方法签名（收/发各几个方法）；任选一个最熟渠道（建议 Telegram 或飞书）跟读一遍收发链路，作面试展开样本。

## 7. Q&A 预测（步5 · Part1 高频题 + Part3 弱点降级 · v2）

### Part 1 · 高频题（四段式答：问题/实现/权衡/局限）

**R07 组**
- Q07.1 [R点基线] 为什么统一七个 IM 渠道？→ 问题=逐渠道接 SDK 使 Agent 核心耦合消息格式/附件/会话模型差异；实现=IMChannel 统一接口+适配器，收发接口统一；权衡=抽象抹平差异 vs 渠道特有能力透传（微信 iLink 仅 P2P、WhatsApp 有风控）；局限=企微/Slack 走外置桥接非原生，不含 JID 寻址路由深水区。
- [JD预测] 各渠道功能完全一致吗？→ 不一致也不假装一致：按公共能力设计统一接口，渠道特有降级在适配器层处理；「七渠道」指接入覆盖，不是七份完整对等功能。
- [JD预测] 新加一个渠道要做什么？→ 写一个适配器实现 IMChannel 接口（收发映射），Agent 核心零改动——这就是 bullet 效果槽「收敛为适配器级改动」的含义。

**R14 组**
- Q14.1 [R点基线] 物化调度解决什么？→ 问题=直接执行模式下重复执行/到期补跑难管理；实现=先把任务物化为 occurrence 记录再消费执行+SQLite 租约互斥+循环 pump；权衡=DB 租约 vs 分布式锁（自托管部署面小、可审计）；局限=60s 粒度下限、不可重入宁可错过不重复。
- [JD预测] 两个实例同时抢任务怎么保证只有一个赢？→ 条件更新+租期判断：UPDATE 带 WHERE lease_expires_at 过期条件，受影响行数定赢家（SQLite 单写者保证原子）；lease_token 是认领凭据，续租/交接凭它校验归属。见 §5-6。
- [JD预测] 为什么最小 60s？不做 1s 行不行？→ 粒度决定 DB 写频率；巡检/提醒/播报类主动值守场景分钟级足够；更小粒度是浪费且放大竞争。
- [JD预测] 任务跑得比间隔还长怎么办？→ 不可重入语义：本轮未结束则跳过下一次触发（租约未释放+心跳续租中），宁可错过这一轮，不并发重跑同一任务。

**R15 组**
- Q15.1 [R点基线] 四段为什么是这四个、为什么正交？→ IDENTITY(我是谁)/SOUL(价值观底线)/AGENTS(工作规则)/TOOLS(工具策略)各自独立变更互不污染；正交=改工作规则不会动到身份底线；append 增量补充但累积冗余、replace 干净但易误删；replace 模式下平台运行时指令不可清除。
- [JD预测] 单人自用为什么需要确认短语？→ Agent 不应能未经确认不可逆地修改自己的核心资产——对话里顺手改坏人设的风险与用户数无关；确认短语是跨轮安全令牌。
- [JD预测] AGENTS 段能做编排吗？→ 不能也不假装：它是声明性工作规则/输出偏好，不是可执行编排引擎；可控性来自结构化+审批流。

**R19 组（v2 新增）**
- Q19.1 [R点基线] 为什么需要常量时间比较？→ 普通字符串比较在首个不同字符即返回，攻击者可借响应耗时推断签名逐字节内容；常量时间比较全程遍历，规避时序侧信道。
- [JD预测] HMAC 签名会话 token vs JWT 用哪个、为什么？→ Opaque 随机 token + 服务端会话表在自托管场景更可控（服务端可即时吊销会话），JWT 自包含但撤销难；加上 HMAC 作防篡改兜底。这是设计取舍；源码实现细节标【待源码确认】。
- [JD预测] 认证和授权什么区别？R19 vs R20？→ R19 管「你是谁」（Web 后台登录认证）；R20 管「你能做什么」（工作区读写删授权矩阵 + IM Owner Gate）。两条相邻但分工明确，R19 不碰运行时身份。
- [JD预测] 你认证做了，session fixation / CSRF / 分布式限流这些 Web 安全这么防？→ §5-13 边界话术：基础 Cookie 三属性（HttpOnly/Secure/SameSite=Strict）已带；我先做「你是谁」这一层；CSRF token 与登录重生成 session 是第二层演进方向（个人自托管后台无并发无资金操作）；分布式限流、验证码与指数退避同理是演进方向，当前应用层内存计数。

**R20 组**
- Q20.1 [R点基线] 为什么 admin 不自动 bypass 工作区所有权？→ 分权模型：admin 管用户与系统配置，数据归属创建者；三态分离 access/modify/delete 判定；配套受控所有权转移路径处理恢复场景（§5-5）。
- [JD预测] IM Owner Gate 具体怎么判？→ 判定链见 §5-4：渠道原生发送者 ID 比对 owner_im_id，不匹配拒执行且不回执防探测；绑定发生在工作区关联渠道账号时。
- [JD预测] 你一个人用，多角色是编的吗？→ §5-3 口径：多用户是设计目标；授权模型单实例可运行，测试用例模拟多角色越权场景验证；运行时多租户硬隔离是演进方向。

### Part 3 · 弱点追问与降级路径（v2）

| # | 弱点追问 | 严重度 | 防线 | 降级路径 |
| --- | --- | --- | --- | --- |
| 1 | lease_token 具体怎么防两个实例同时跑？（到 SQL 原子性） | 高 | §5-6：条件更新+租期判赢家/token 凭据校验/心跳续租 | 接不住 HOW → 退「设计原理层」：互斥靠 DB 条件更新非应用锁，宁可错过不重复；再退 → 转 R15/R20 工程层 |
| 2 | IM Owner Gate 比对失败后 deny 流程长什么样？owner_im_id 从哪来？ | 高 | §5-4 完整判定链（含不回执防探测的理由） | 接不住 → 泛化为「渠道身份匹配」授权判断层，承认运行时隔离是演进方向，退守 R20 三态矩阵主体 |
| 3 | 多用户平台你服务了几个用户/租户？ | 高 | §5-1 世界观头罩 + §5-3 设计目标口径 | 无需技术降级，这是叙事题——诚实+不心虚即可 |
| 4 | admin 不能碰数据，工作区主人失联谁接管？ | 高 | §5-5 受控所有权转移+承认转移机制完整实现是演进方向 | 接不住 → 「当前实现准入级三态+初始绑定，接管在路线图」——头转成演进方向不硬啃 |
| 5 | 你的登录 token 到底是签名还是随机 id？密钥怎么保管？（R19 内部矛盾 · v2 上调） | **高** | §5-12 自洽链：随机 token 存会话表 → HMAC 作防篡改兜底 → 常量时间比较 → Cookie → 限流；密钥走环境变量、轮换=换 key+清会话表 | 接不住 → 退「取舍层」：真 opaque 随机 id + 服务端会话表可即时吊销，HMAC 只是防篡改兜底；再退 → 转 R20 授权矩阵 |
| 6 | session fixation / CSRF 这些 Web 安全怎么防？（R19 连招 · v2 新增） | 高 | §5-13：基础 Cookie 三属性已带；CSRF token/登录重生成 session 属第二层演进方向；个人自托管后台无并发无资金操作所以没上 | 无需死磕——诚实画界「先做是谁这一层，第二层是演进方向」即可 |
| 7 | 分布式限流怎么防绕过（换一堆代理扫号）？（R19 · v2 新增） | 中 | 自托管单实例应用层内存计数；分布式限流/验证码/指数退避是演进方向；真实 IP 需可信代理取 X-Forwarded-For | 退「自托管边界=演进方向」叙事题，不硬卷实现 |
| 8 | 用户 A 能不能读用户 B 工作区对象？（R20 对象级 · v2 新增） | 中 | §5-11 认证/授权分界 + 补充：我实现的是工作区/角色级授权判定（canAccessGroup 中间件层判 group），对象级（行内资源）是演进方向；R19 只认登录、R20 只认授权判定，都是准入级 | 退「准入级 vs 对象级」边界；承认对象级未做但引为演进方向 |
| 9 | 你凭什么叫数字员工平台、凭什么说质量有保障？（评测降级 · v2 更新） | 中 | §5-14 轻量评测声明 + §5-8（工具调用留痕结论可追溯）+ 每个机制自带验证（R14 并发用例/R15 replace 不可清除/R20 越权用例）→ 可信赖=安全双支柱+留痕+自带验证 | 被深挖评测方法论 → 诚实「前沿认知、未实现、无实测数据」，退 R14/R20 工程层 |
| 10 | 你的工具调用链路怎么实现的？（JD 原话，bullet 外） | 中 | §5-8：SDK 托管执行循环+我做策略治理与全链路留痕两层 | 追 SDK 内部 → 诚实画界「SDK 托管部分以使用与观测为主」，转讲 TOOLS 段治理与轨迹归因 |
| 11 | React 用在哪？（技术栈悬空雷） | 中 | §5-10：实时状态（虚拟化长列表/流式渲染/断线重连） | 追前端工程细节 → 承认重心在服务端与机制层 |
| 12 | 为什么没有 RAG？客服不都要知识库吗 | 中 | §5-9：确定性数据走工具调用有证据链，开放问答向量检索是演进方向 | 无需再降——这是取舍叙事不是能力缺口硬伤 |
| 13 | 你说多副本安全调度，真验证过吗？（R14 + R25 移除连带 · v2 新增） | 中 | §5-6 lease HOW + §5-2 多实例口径：互斥靠 DB 条件更新非应用锁、SQLite 单写者保证原子——原理讲得清、并发用例在测试套件验证过；极端时钟漂移压测没做，如实说 | 退「设计原则+局部用例回归」，不死磕「我证明它完美」 |
| 14 | V2 的 V1 是什么？（若被问版本史） | 低 | §5-7 materialization 动机叙事 | bullet 已删 V2，仅在主动问及时用 |
| 15 | 七个渠道你都真实接过消息吗？ | 低 | 诚实：以飞书/Telegram 等主渠道验证过收发链路，其余渠道按同一接口实现并有契约测试兜底 | 无需降级，Scope 本就如此 |

## 8. 步1 核对表（v2 更新）

| Bullet | 四段式完整 | 验证效果定性 | Scope 越界风险 | roadmap 写成已实现 | Q 编号 |
| --- | --- | --- | --- | --- | --- |
| R07 七渠道抽象 | ✓ | ✓ | ⚠️ 中：不写"功能完全一致"/企微原生（已守） | 无 | Q07.x |
| R14 定时调度 | ✓ | ✓ | ⚠️ 低-中：「多实例」需口径（§5-2 已备） | 无 | Q14.x |
| R15 Profile 四段式 | ✓ | ✓ | ⚠️ 中：AGENTS≠编排引擎；无 RAG（已守） | 无 | Q15.x |
| R19 认证 | ✓ | ✓ | ⚠️ 低：仅 Web 用户认证，不碰运行时身份/IM 授权 | 无 | Q19.x |
| R20 RBAC 三态 | ✓ | ✓ | ⚠️ 高→已处理：删「企业多租户隔离」+授权矩阵话术 | 无 | Q20.x |

## 9. 步1 风险预埋（历史 · 已被步2-5 消化 · v2 更新）

1. ✅ 叙事风险：步2 世界观声明 + 头罩机制落地。
2. ✅ R20 效果槽收窄：删「企业多租户隔离」+ 换授权矩阵专属话术。
3. ✅ 超档补课：升级为 §6 源码补课清单（五项带读计划）。
4. ✅ 关键词映射：步2 §2.4 展开 + 补齐「工具调用/会话状态」缺口。
5. ✅ **v2 评测降级**：R25→R19，评测思想降为轻量业务 Review + 典型 Case 回归（§5-13），简历不出现评测底座。

---

## 执行进度

- [x] 步1 打底（§0/§1/§8/§9）
- [x] 步2 生成场景（§2 场景包）
- [x] 步3 对齐改写（§3 改写 bullet+talking+映射表）
- [x] 步4 对抗审查（§4 审查记录：v1 四审查员 + v2 R25→R19 变更与归属检查）
- [x] 步5 Q&A 预测（§7 Part1 五组 + Part3 十一条弱点降级）

> **选点 v2 完成（R25→R19 + 评测降级）。** 五条 bullet 稳定在 R07/R14/R15/R19/R20，安全双支柱（认证+授权）收尾。R19 已按 D 终审补齐全部新点雷（token 自洽链/密钥管理/Web 安全第二层边界/分布限流），严重度上调为高；R20（超档）+R14（超档）仍需源码补课（§6 优先）。
> 剩余全局事项：**① 步6 三项目联动**——「评测对比口径」需更新（Craft R25 行为评测对比对象已不存在，HappyClaw 不再有评测点，评测演进线「工程验证→行为评测」断链，需改为「每机制自带验证 + 轻量回归」口吻）；**② 步7 简历文件**——删 R25 bullet、改简介「行为质检/可评测」→「认证授权与权限保障」、加 R19 bullet、防线同步；**③ 源码补课带读**（Craft §6 / Happy §6，§6 新增 R19 第 3 项）。
