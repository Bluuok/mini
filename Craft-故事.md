# ThreadCove：把一次研究做成可检查的执行链

> 2026-09-19 修订。完整技术说明与五点分时长讲稿见[八模块准备稿](修改/CraftAgent/Craft-故事.md)，源码固定为 `Bluuok/ThreadCove@6a49d19a024fcbc6cdabeaf8da8ccbf1953dfbb9`。本轮事实和验证范围见[复审记录](修改/复审-2026-09-19/README.md)；[2026-09-18 记录](修改/复审-2026-09-18/README.md)保留当时结论。

## M1 · 事实卡与开场

使用者是自己，任务是技术调研、资料阅读和方案比较。本人已确认：ThreadCove 是基于 Craft Agents 开源基座、围绕自己选定的五个核心点复现并实现的项目，五点均作为个人实现讲解。具体对象是来源、工具、子问题、上下文、运行状态和已保存内容；第三方 SDK、参考架构与 AI 辅助的作用另行说明。

**可口述开场：**

> ThreadCove 是我基于 Craft Agents 开源基座，围绕五个选点复现并实现的个人研究工作台。我想解决的是：技术调研除了最终答案，还需要知道资料来自哪里、哪些子问题已经完成、中断后留下了什么。Pi 负责提出搜索、网页读取和可选委派请求，我在宿主实现工具执行、来源保存、并发与累计预算控制、取消及报告收集。工作台还通过公共后端接口接入不同 SDK，Claude 路径单独做字符预算历史桥接。任务层区分受理和完成，用稳定消息 ID、快照和检查点保存内容，重启时标记未完成运行。五点共同让研究过程可检查、可约束；研究结论是否受到来源支持还需要进一步核验。

## M2 · 一个贯穿例子

【演示设计】输入：“比较框架 A 与 B 的工具扩展、取消机制和部署限制；给出来源，没有证据的部分明确标注。”这是待执行的演示题，不冒充已完成的研究报告。

搜索与阅读是动态取材；拆不拆任务由请求内容和模型决定，不是每次都强制三个子任务。输出是比较结论、来源线索和缺口，而不是自动判定哪一个框架绝对更优。保存 URL 证明有取材线索，不能直接证明每个论点已被来源支持。

## M3 · 实际链路与旁支

```text
研究问题 → SessionManager 受理与 runId
         → Pi 后端 → 搜索/读取工具 → 宿主保存取材
                    └ 可选 delegate_research → 叶子子代理 → 子报告回填
         → AgentEvent → RunTranscript → UI / 保存文本与状态

Claude 后端 → 字符预算历史桥接（另一条适配路径）
```

**两个顺序不能混为一谈：**简历五条是能力目录，不是一条研究请求的五步执行计划；架构解释顺序也不是本人真实开发时间线。后端、工具和持久化在运行时协作，不要求把一切画成严格线性依赖。

| 阶段 | 输入与动作 | 可观察输出 | 未覆盖范围 |
|---|---|---|---|
| 受理 | requestId、会话忙碌检查与状态保存 | accepted/runId | 受理不等于成功 |
| 取材 | 查询或 URL，宿主执行 | 带来源/时间的 JSON 产物 | 不保证证据支持全部结论 |
| 委派 | 可独立的子问题及共享预算 | 每个子任务状态、来源、报告 | 非通用 DAG；非跨机配额 |
| 保存 | 文本/工具结果/错误事件 | 稳定消息 ID、快照、检查点 | 非全事件无损日志 |
| 重启 | 已保存会话与子报告 | 内容可回看，残留执行标中断 | 不主动重放旧模型或网络调用 |

## M4 · 五条简历对应的技术故事

### M4.1 研究工具与来源

问题不是“还缺一个 MCP 名字”，而是工具调用后缺少可回看的研究材料。宿主保存查询或页面 URL、抓取时间和截取正文，网页读取检查公开地址、重定向与规模限制。代价是只保留受限观察；网页标注不可信也不保证模型永不受注入影响。对应[QA Q01](修改/CraftAgent/Craft-面试QA.md)。

### M4.2 受限委派

独立问题可以并发，但无限递归会放大调用量和取消复杂度。我实现运行时共享并发槽、父运行累计子任务数与网页工具调用预算，并分别限制子任务模型轮数。网页预算计数的是 web_search/web_fetch 调用，不能当作物理 HTTP 次数或费用封顶。子代理只拿搜索、读取工具，执行器拒绝再次委派；子代理有独立工作目录，**研究产物与报告由宿主写回父会话目录**。对应[QA Q02](修改/CraftAgent/Craft-面试QA.md)。

### M4.3 Claude 历史桥接

这是多后端工作台的配套能力，不是 Pi 搜索之后的“统一记忆阶段”。Claude 按字符预算保留近期有效角色文本；Pi 当前回填 user/assistant，预算与工具历史未对齐。这里可讲保留策略、丢弃代价和包装开销，不讲精确 token 预算或长期语义记忆。对应[QA Q03](修改/CraftAgent/Craft-面试QA.md)。

### M4.4 后端适配

统一的是调用和生命周期接口，新增后端仍要适配。Pi SDK 在 JSONL 子进程运行，工具请求回宿主；模型凭据进入 SDK 所在子进程，与 Source 工具凭据分开。隔离依赖职责不等于 OS 沙箱。对应[QA Q04](修改/CraftAgent/Craft-面试QA.md)。

### M4.5 运行状态

网络断线不等于任务失败，RPC 返回也不等于任务成功。会话层把受理、事件和终态分开，RunTranscript 保存可更新的文本记录。恢复的是已经落盘的内容；检查点之间可能丢尾部，不自动重做旧任务。对应[QA Q05](修改/CraftAgent/Craft-面试QA.md)。

## M5 · 主故事的深入讲法

项目总述使用开场，再根据追问选择一个核心点。[完整故事 M4](修改/CraftAgent/Craft-故事.md#m4--五个技术故事与简历逐条对应)为五点分别提供 30 秒、90 秒与约 3 分钟连贯讲稿；技术路径用于支撑讲述，不替代问题、实现、取舍与结果。

【机制推演】父任务取消时，一些子任务在运行、一些仍排队，需要分别处理退出等待、调用销毁、释放槽和保存状态。普通子任务失败形成失败报告；报告写盘失败可使委派调用整体报错，`allSettled` 先等待各 runChild 落定再交回错误，清理由各子任务的 finally 执行。等待 Promise 落定本身不证明底层进程或外部请求已经停止，具体运行效果属于验证范围。

个人贡献按五点复现实现讲述，再选工具契约、预算作用域或检查点中的一个接口解释取舍。开发时间线和真实运行记录集中放在事实审计，机制推演不改写成亲历生产事故。

## M6 · 八轮反向追问入口

| 主问 | 再问 | 回答落点 |
|---|---|---|
| 为什么需要研究工具？ | MCP 不能直接做吗？ | 连接能力与来源产物契约分开 |
| 为什么拆任务？ | 简单问题也拆吗？ | 可选独立子问题，不按数量凑 |
| 子代理隔离什么？ | 报告存哪里？ | 独立 cwd；宿主保存到父会话 |
| 失败会吞结果吗？ | 写盘失败呢？ | 常规失败与持久化失败不同 |
| 字符预算解决什么？ | Pi 也有吗？ | Claude 旁支，不能统一扩大 |
| 重启能继续吗？ | 外部请求重复吗？ | 内容与状态恢复，不自动续跑 |
| 怎么知道做对了？ | 跑过哪条用例？ | 源码、测试定义、运行记录分开 |
| 哪段是你做的？ | SDK 和 AI 做了什么？ | 五点由本人围绕选点复现实现；区分应用集成与 SDK 内部循环 |

每轮的简答、进一步展开和不会时的诚实答法见[完整故事 M6](修改/CraftAgent/Craft-故事.md#m6--失败场景与八轮二层追问)，可逐轮演练。

## M7 · 与 Happy 的互补

ThreadCove 解释研究工具、受限委派与保存；Clawtide 解释消息/计划入口、配置与资源归属。两者共享的是工程问题的思考方式，不能由此推断共享代码或时间上的先后因果。详见[跨项目联动](三项目联动-步6.md)。

## M8 · 概念与证据入口

Harness 是工具面和上下文组织；Runtime 边界说明在哪里执行与怎样停止；Trace 是记录，Eval 是带标准的质量判断。Session 存储不等于模型已恢复上下文；URL 不等于逐论点证据。

[完整机制与概念速查](修改/CraftAgent/Craft-故事.md) · [既有证据导航](修改/CraftAgent/Craft-场景与证据审计.md) · [本轮裁决与验证范围](修改/复审-2026-09-19/README.md)。
