# 三项目联动 · 步6 定稿（Story Module 7 · 步6 评审修订版 · v3 换点同步）

> 生成：2026-08-24｜模型档：Fable 5（手册步6 要求 Sonnet/Opus，达标）｜二轮：双审查员对抗评审（A 真实性 78 / C 实战 79）裁决后修订。
> **v3 同步（2026-08-29）**：Craft R24→R10 换点（分层验证→事件流）+ HappyClaw R25→R19 v2 补记——§1 表/复用线 D E/三分钟稿/Round 7 8/N4 已同步换点后口径。
> 输入：`MiniCode-定稿.md` / `Craft-定稿.md` / `HappyClaw-定稿.md` + 14 轮跨项目追问树（子代理产出，已校订）+ 评审修订。
> 红线：三项目**非同源**——只连能力成长，绝不共享代码库/服务/模型/接口；每模块标真实性标签；论文数字只当前沿认知。
> ⚠️ 两条例外纪律（评审新增）：
> **口述纪律**：面试中绝不出内部引注（「见铁律几」「复用线几」「Rxx 清单」都是背稿信号）——本文件的编号体系只给自己看，出口的只有人说的话。
> **降维纪律**：所有「体系感」总结（三步演进/三层递进）临场必须降维成个人化的「我当时是这么发现的」，工整叙事从 L2 嘴里说出来反而可疑。

---

## 0. 三条铁律（出错即崩，全场贯穿）

1. **零共享**：三个项目没有共享代码库、服务、模型接口。被问「从 MiniCode 带了多少代码过来」→「代码零复用，带走的是方法论。」证据锚在**无共享代码库**而非「技术栈完全不同」：主体技术与运行环境不同——Craft=Electron+Bun+WebSocket+MCP，HappyClaw=Node+Hono+SQLite+Docker+Agent SDK，两者虽共用 TS/React 生态但没有一行共享代码。连执行循环都不同源：MiniCode 自己写循环编排，HappyClaw 用 Claude Agent SDK 托管循环、我只做治理与留痕。
2. **效果不报数**：不报营收/使用量/性能数字；pass^k、LLM 裁判、MT-Bench 只作前沿认知（未实现无数据）；290+ 测试这类**构建事实**只能作为「做了这些用例」的证据，不能包装成「效果好」。
3. **编号互不映射**：被问「三个项目的 R08/R18/R10 是一套体系吗」→「那是我整理选点时的内部标签，不是一套体系——MiniCode 的 R08 是工具契约，Craft 的 R08 是任务隔离；最典型是 R10：MiniCode 的 R10 是写前确认 diff，Craft 的 R10 是 AgentEvent 统一事件流与生命周期，完全是不同的东西。」然后直接答实质，不加「我们看实质」这类过渡腔。

---

## 1. 能力演进时间线【主线：执行与契约 → 工程化 → 多用户产品化】

| 项目 | 身份 | 问题域切面 | 该段新增能力（前一段没有的） | 标签 |
| --- | --- | --- | --- | --- |
| MiniCode 研发协作助手 | 实习 | **执行与契约层**：「一个 Agent 怎么干活且可控」 | Agent 执行循环（模型-工具-模型、结果回填、maxSteps 终止保护）、工具参数契约（Schema+运行时双校验）、受控文件观察（目录限制/分页/输出上限）、写前确认 diff、JSONL 会话恢复与探索分支 | 循环/契约/写前确认/会话记录【本人实际参与·参与实现】；文件工具受控观察【本人实际参与·独立实现该子系统】 |
| Craft Agents | 个人 | **工程化层**：「一个 Agent 工程怎么扩展、怎么不崩」 | 模型后端解耦（AgentBackend/provider-agnostic 接口）、研究任务 Session/Workspace 隔离、外部能力统一接入（ServerBuilder：MCP/API/本地源→工具）、IPC/WebSocket 统一传输多端复用、AgentEvent 统一事件流（流式渲染/错误恢复契约，v3 换点由分层验证换入） | 五条均为【本人实际参与·个人独立开发】；能力描述以各点 Scope Boundary 为界 |
| HappyClaw | 个人 | **多用户产品化层**：「一个 Agent 怎么对多角色、可治理、可信任」 | IM 七渠道统一入口（适配器模式）、定时主动值守（物化调度+SQLite 租约互斥）、人设四段式治理（四段正交+确认短语）、后台登录认证（Cookie Session+防爆破）、RBAC 分权（三态+admin 不 bypass+IM Owner Gate） | 五条均为【本人实际参与·个人独立开发】；「多用户=设计目标」口径已在 HappyClaw 定稿 §5-3 定死，防线已备 |

**一句话演进逻辑**：从「我会做一个 Agent 工具」（执行闭环可控）到「我会做一个 Agent 工程」（可扩展防崩）到「我会做一个 Agent 产品」（可治理可质检）。每一层引入前一层没有的能力——递进关系，不是三个并列 demo。（口述时降维：「第一个项目教会我 Agent 怎么跑起来不失控；第二个让我敢把它做成一个能一直改下去的工程；第三个才碰到真正的产品问题——多人、权限、出了错怎么查。」）

【待确认·最高优先】**真实实习与 MiniCode 的关系必须先确认**（比时间线更前置）：若真实实习内容与 MiniCode 无关，实习身份的全部表述须按真实情况重写，而非微调措辞。其次才是：三段真实起止时间与先后顺序——建议叙事顺序=实习在前（打基础）→ Craft（工程化延伸）→ HappyClaw（产品化收口）；若真实顺序不同按真实调整，保持「能力递进」框架不变。

---

## 2. 方法复用线（同一判断的三次落地——复用的是决策原则，不是代码）

> 使用规则：临场**优先讲 B 或 C**（各有三次干净落地）；A/E 作补充；**D 只在被直接问到「事件流是不是同一个」时用大白话认账，不主动展开**。

### 复用线 A · 「别让 Agent 无限膨胀」：受控读取 → 受控执行
- MiniCode R09：无界读取让上下文与成本失控 → 工作目录限制+分页读取+输出上限【本人实际参与】
- HappyClaw：SDK 托管的循环里工具调用留痕（入参/出参副作用可追溯）→ 受控可追溯【本人实际参与】
- 口径：同一个「要可控、要留痕」的判断，一个落在**读取规模**，一个落在**执行可追溯**。
- 口语版：「一开始我发现它读文件没有节制，上下文和钱都烧得快，就给读取上了限制；到第三个项目，工具是 SDK 提供的，我要管的是『每次调用留没留痕、副作用能不能查』。」

### 复用线 B · 「工具要有边界」：契约 → 接入 → 治理（主推）
- MiniCode R08：自建文件工具 → Schema 给生成端引导+运行时校验兜底（**契约**）【参与实现】
- Craft R18：工具来自外部 MCP/API/本地源，schema 上游自带 → ServerBuilder 统一转换+凭据注入（**接入**）【本人实际参与】
- HappyClaw：工具由 SDK 托管 → Profile TOOLS 段声明式策略+调用全链路留痕（**治理**）【本人实际参与】
- 结构化口径：「问题域从自己造工具→接入外部工具→治理现成工具，方案跟着问题变，不是推翻自己。」
- **口语降维版（临场用这个）**：「我一开始自己写工具，参数校验得自己管两头——给模型看的描述和真正执行前的检查；后来做第二个项目，工具是从外面接进来的，人家自带说明，我就不用写了，改成把它们统一接进来；到第三个项目，工具干脆是 SDK 给的，我要管的变成『怎么管住它』——哪些能用、每次调用留没留痕。」

### 复用线 C · 「不可逆变更须人确认」：diff 预览 → 确认短语（主推）
- MiniCode R10：文件写入先出统一 diff、用户确认才落盘（对象是结构化的行级改动 → diff 信息量最高）【参与实现】
- HappyClaw R15：人设修改用跨轮确认短语（对象是抽象的身份/规则资产，没有自然 diff → 用安全令牌）【本人实际参与】
- 口径：共享的是「确认由人完成、不让模型自行通过」的原则；呈现形态随对象变。
- 追问备答：「diff 能不能用在人设上？」→「硬做就是贴前后两整段 prompt，信息量很低，不如确认短语直接。」

### 复用线 D · 「状态管理的三种形态」（⚠️ 仅被动应答，不主动讲）
- MiniCode R18：JSONL 逐事件追加 → **可恢复**（崩溃恢复+单用户探索分支）【参与实现】
- Craft R08+R10：Session/Workspace → **可隔离**（多研究任务并行互不污染）；AgentEvent 事件流 → **可观察**（长链路执行实时呈现、错误可恢复）【本人实际参与】
- HappyClaw：全链路事件轨迹 → **可归因**（行为变化定位到具体改动）【本人实际参与】
- ⚠️ 评审修正：原版「状态要有记录」主题不齐（Craft 这腿是隔离不是记录），且「被认出相似恰恰证明方法论贯穿」这种金句像背台词——**删**。被动应答用大白话认账：「对，这几个项目里都有类似事件流的东西，但一个是存会话好回放、一个是划边界防串台+把执行过程实时摆出来、一个是留痕迹好查账，干的活不一样。」

### 复用线 E · 「工程安全」递进：事件契约 → 授权与认证基石（作补充 · v3 更新）
- Craft R10（+R12 路由穷举）：统一事件契约（结构化错误带恢复入口）、传输通道分类穷举——回答「改代码不会把自己写崩」（契约锁死+结构化恢复）【本人实际参与】
- HappyClaw R19+R20：登录认证（你是谁）+ 授权矩阵（你能做什么，含 IM Owner Gate）——回答「Agent 的行为边界可控、数据归属清晰」（安全双支柱）【本人实际参与】
- 口径：两项目都把「安全/质量」当工程问题而非模型问题——Craft 锁数据契约与呈现边界，HappyClaw 锁行为边界与数据归属。
- **口语降维版**：「我个人项目没人帮我回归测试，第一反应是把『别把自己改崩』锁死——事件长什么样、通道怎么分类，这些契约先定死，出错时有结构化的信息能恢复，而不是一行红字全断；到第三个项目，面对的是一堆人可能共用、可能有人乱动配置，我要锁的是『谁能动什么』——认证加上授权。」

---

## 3. 三分钟叙事完整稿（「为什么这三段证明我适合 AI 应用开发岗」）

> 结构：总起一句 → 三段各 45 秒（难点→做法→教训）→ 收束。背骨架不背字；归属动词严格对齐简历。

**总起**：我用三个项目回答「AI 应用开发到底难在哪」——它们覆盖了接入→工程→治理→信任边界的完整生命线，且是递进的三个切面，不是一个东西做三遍。

**第一段 · MiniCode（实习）**：难在「Agent 不是跑一次，是多步闭环还要可控」。我**参与做了**执行循环、工具参数契约、写前确认和会话记录这几块——方向是团队定的，我落在实现和细节上；其中文件工具的受控观察（目录限制/分页/上限）是我**独立实现的子系统**。教训：没终止保护的循环会失控，没校验的工具会直接作用于文件系统，没确认的写入会误改团队文档。

**第二段 · Craft（个人）**：难在「一个 Agent 工程怎么扩展、怎么不崩」。我做模型后端解耦（按任务阶段换模型）、Session/Workspace 任务隔离、ServerBuilder 把 MCP/API/本地源统一转成工具、IPC/WebSocket 一套传输逻辑多端复用、统一事件流（长链路执行实时呈现、错误结构化可恢复）。教训：单人高频迭代更需要把契约和边界锁死，否则 bug 追不回。

**第三段 · HappyClaw（个人）**：难在「Agent 要成为产品，怎么对多角色、可治理、可信任」。我做七渠道统一入口（适配器）、定时主动值守（物化调度+租约互斥）、人设四段式治理（确认短语）、后台登录认证（Cookie Session+防爆破）、RBAC 分权（admin 不自动 bypass 所有权+IM Owner Gate）。教训：多角色产品里，身份和边界必须一进平台就锁死——认证管「你是谁」，授权管「你能做什么」，两者合起来才是可信赖。

**收束**：我不报虚数、不虚构使用方，我能讲清的是每个项目里的取舍和边界。这三段合起来：能把 AI 应用从「跑通」做到「工程化」再做到「可治理、可信任」——正是这个岗位要的全链路。

---

## 4. 跨项目追问树（16 轮 · 含评审新增 4 题，按危险度排序）

### ★★★ 必背六题

**Round 1｜「你就是把 Claude Code 复述了三遍」（全场最高危）**
- 追问链：复述有什么意义 → 换谁都行凭什么算你的能力 → 你是不是在回避「算不算创新」。
- 30 秒答：Claude Code 是业界典型的形态，不是我的作品。我做的是拿三个不同的问题域去理解 AI 应用怎么落地——内部的文档协作、个人的深度研究工作台、还有一个自托管的数字员工平台（跨境客服运营是我给自己配的演示场景）。范式是共通的，但属于我的是每个项目里具体的设计取舍。
- 退路：「我的价值不在会调 Agent，在取舍能不能被问倒——比如双份 Schema 的漂移风险、MCP 为什么只做转换层、admin 为什么不 bypass。」

**Round 2｜「参与实现执行循环，你到底写了哪个文件」（实习归属敏感题 · 评审修订版）**
- 口语主答（**不报内部编号清单**）：「那个文件工具的受控观察——目录限制、分页读取、输出上限——是我独立实现的子系统。执行循环、工具契约那几条是团队定了方向，我落在实现和细节上。具体模块名以当时仓库为准。」
- 禁忌：不说「独立负责」（隐含所有权，邀请「就你一个人做这块？」）；只说「独立实现该子系统」，并永远带团队定语「方向与验收是团队共同定的」。
- 反向防御：被问「你一个人做了整个产品？」→「不是，那是两个个人项目；实习这段我的范围就是这些子系统。」
- ⚠️ 最高优先【待确认】：真实实习与 MiniCode 的关系未确认前，此题答案悬空——团队细节只能从真实实习长出来。**MiniCode 细节回看项（面试前必做）**：受控观察三要素的实现位置、diff 的计算方式、JSONL 事件的具体结构——能口头重述即可撑住「独立实现」四个字。

**Round 5｜「这三个项目是不是共用了一套底子」（红线检测 · 评审修订版）**
- 30 秒答：零共享。三个项目的主体技术和运行环境都不一样，连执行循环都各是各的——第一个项目我们自己写循环，第三个项目循环是 SDK 托管的。有些概念长得像，是因为我用同一套工程思维在做不同的事，不是搬代码。
- 追问「带了什么过来」→「只带走了方法论，代码一行没带。」
- 退路：被抓住某个相似点就拆开讲差异（见复用线 D 大白话版），先认账再区分。

**N1｜「你说跨境电商数字员工，到底有没有真实客户在用？还是就你自己部署？」（评审新增 · 权重最高）**
- 30 秒答（直接继承 HappyClaw §5-1+§5-3）：「这是我个人开发的自托管平台——支持多用户多角色的产品形态，跨境客服运营是我给自己配的演示垂直场景。多用户是设计目标：授权模型单实例就能跑，我用测试用例模拟多角色越权验证到了工作区/角色级；运行时多租户硬隔离是平台化之后的演进方向。」
- 纪律：这是叙事题不是技术题，诚实+不心虚即可；绝不含糊其辞暗示「有企业在用」。

**N2｜「开源框架已经帮你做了执行循环和工具封装，你的增量在哪？」（评审新增 · 必背）**
- 30 秒答：「框架管的是通用执行这一层。我的增量在上面两层：一是治理——比如数字员工能用哪些工具、用法边界是什么，我用声明式的 Profile 配置管住；二是评判——每次调用的出入参我都留了痕，改了什么导致行为变化能查回去。Craft 里同理：Pi 只是接进来的第二个模型后端，真正的增量在设计层——外部能力的统一接入、多端复用这些。」
- 纪律：讲「我消费框架做什么」，绝不认领框架内部实现。

**Round 14｜「一句话总结这三段为什么适合这个岗」（收尾 · 评审修订版）**
- 用 §3 收束句：「从跑通到工程化再到可治理、可信任——岗位要的全链路，我从三个切面各做了一段。」
- 被问比别人好在哪 → 不贬低别人：「我的三段是递进不是并列。」
- 被问哪段最能挖 → **分层承诺（不过度承诺）**：「三段的取舍我都能讲清；深度上看我对哪块源码最熟——目前最能往下挖的是第一个项目的执行循环和第三个项目的分权模型。」

### ★★ 理解即可十题

**Round 3｜时间线怎么排**【待确认】：实习白天主线、个人项目业余做的口径；不硬撑强度，「广度覆盖、深度挑重点啃」；真实起止时间面试前回填。

**Round 4｜为什么做三个不做深一个**：数量不是优势，三个**不同维度**正好对应岗位完整链路；真要删会删与岗位重合度最低的，但保留成长线。

**Round 6｜「MiniCode 用 Schema+Zod，后面为什么换了」（降级为理解）**
- 压缩 30 秒：「不是推翻，是问题变了——工具从自己写、到外面接、再到 SDK 给，方案跟着问题走。」
- 展开储备：自建工具需要手写契约（Schema 引导+运行时校验）；外部工具上游自带 schema，只需 ServerBuilder 统一转换；SDK 工具重心变成治理。退路：「双份定义的漂移风险当时就知道，没做自动生成是优先级取舍。」

**Round 7｜「Craft 和 HappyClaw 都有工程质量，差别是什么」（降级为理解）**
- 要点：Craft=数据契约（事件契约+通道分类穷举，锁「数据形状和呈现边界别崩」，错误有结构化恢复入口）；HappyClaw=认证+授权双支柱（锁「谁能动什么、数据归属清晰」）。两个把「安全/质量」当**工程问题而非模型问题**，但一个锁数据契约、一个锁行为边界与数据归属。
- 被问质量量化 →「HappyClaw 评测不单独成点，只做轻量业务 Review + 典型 Case 回归两个轻动作；LLM 裁判/任务成功率是方法论认知，未实现无实测数据。Craft 同理——事件流是行为可复现的数据基础，不是行为评测本身。」

**Round 8｜三套 R 编号是不是一套**：见 §0 铁律 3。记混时诚实承认「内部标签」并转实质。

**Round 9/10/11｜能力成长+方法复用+确认机制迁移**：储备复用线 **B 或 C** 一条讲透即可（口语降维版已备）；D 只被动应答。

**Round 12｜实习参与 vs 个人独写，信哪个**：互补不互贬——实习证明真实团队环境里把子系统做扎实，个人项目证明端到端判断力；都不夸大。
- 追问「实习学的和自学做的哪个更难」→ 诚实选边：「实习难在别人定了方向你要做扎实，自学难在判断没人帮你兜底。」按真实感受讲。

**Round 13｜最有主见的一次技术决定 + 犯过的错（错误故事升级为重点储备）**
- 主推：HappyClaw 分权模型（admin 不 bypass+受控所有权转移配套）。
- **必备用「犯过的错」故事**：「MiniCode 双份 Schema 我当时就知道有漂移风险，没做单一事实源自动生成——这是我承认的优先级取舍，也是现在会改的做法。」诚实+成熟信号。

**N3｜「三个项目最不满意哪个？重做会改什么？」（评审新增）**
- 选 HappyClaw 但演成成长：「重做的话我会先把『认证 vs 授权』的分界想清楚再动手——这次是做到一半才意识到这俩是两层，得分开设计，下次开工第一天就把访问控制模型画出来。」顺带带出 MiniCode 双 Schema 改进，两个改进故事一举两得。**绝不挑最弱项自我贬低。**

**N4｜「个人项目搞这么多工程体系，是不是过度设计？」（评审新增）**
- 直接用 Craft §5-6 单人防御叙事（v3 重锚）：「我一个人维护、迭代快，更需要把契约和边界锁死，否则 bug 追不回——事件契约、通道分类这些边界一次锁对，后面高频改的是实现不是契约。这不是团队规模决定的，是单人高频迭代决定的。」再补一句「这套底座是可复用的，研究/数字员工只是第一个垂直场景」。这是叙事牌不是技术题。

**N5｜TS 现场压测（如 refine 与 superRefine 区别）（评审新增 · 低优先）**
- 补到了就答：「refine 只返回布尔，superRefine 能收集结构化错误、支持二次 parse。」
- 没到就诚实降级+转动机：「我是在做双份 Schema 时意识到执行端校验需要结构化错误才能生成 diff 提示，这块还在补。」切忌现场硬编。

---

## 5. 使用建议与待办

**必背六题**：Round 1（复述三遍）/ Round 2（实习归属·新版）/ Round 5（零共享）/ N1（多用户真实度）/ N2（框架增量）/ Round 14（总叙事）。
**重点储备**：Round 13 错误故事、N3 重做故事、复用线 B/C 口语版任选一条。
**口述纪律**：一切内部引注（§/铁律/复用线/Rxx）只进脑子不进嘴。

**面试前必须回填的【待确认】清单**（均只有用户能填）：
1. **最高优先**：真实实习与 MiniCode 的关系确认（公司/行业/规模/部门/团队人数/IM/真实职责/协作方式——MiniCode-定稿.md §六全槽位）。若无关则实习表述重写。
2. 三项目真实起止时间与先后顺序（Round 3 口径用）。
3. 实习期间与两个个人项目的时间分配说法（确保与真实一致）。

**全局剩余事项**：步7 简历 HTML/PDF；Craft/Happy/Mini 三份源码细节回看（Craft §6 / HappyClaw §6 / MiniCode Round 2 回看项）。

---

## 6. 步6 评审记录（2026-08-24 双审查员）

| 审查员 | 维度 | 评分 | 核心裁决 |
| --- | --- | --- | --- |
| A 真实性 | 虚构关联/实习身份/一致性 | 78 | ① 复用线 D 主题不齐最牵强；② R09「独立负责」过重+实习匹配未定+MiniCode 无回看清单=最高危敞口；③ Round14 过度承诺；④ 三分钟稿「我做」vs「参与实现」张力；⑤ Round1 缺自托管头罩；⑥ 技术栈措辞不精确（共 TS/React） |
| C 实战 | 背稿感/L2可信度/缺失追问 | 79 | ① **漏最高危「多用户真实度」题**；② 漏「开源框架增量」题；③ 内部引注=背稿信号；④ 工整演进叙事 L2 超档须口语化；⑤ 必背重排（6/7 降级、13 错误故事升级）；⑥ 补 N3 重做/N4 过度工程/N5 压测 |

**修订采纳**：全部执行——复用线 D 改「三种形态」仅被动应答+删金句；R09 统一「独立实现该子系统」+团队定语+新增 MiniCode 回看项；Round14 分层承诺；Round1 补头罩；铁律1 锚「无共享代码库」；新增 N1-N5 五题；必背重排为 Round 1/2/5/14+N1/N2；全篇剥离口述内部引注；B/E 加口语降维版。
