# MiniCode 定稿（步3 改写 + 步4 修订 + 步5 Q&A 全产物）

> 生成：2026-08-24。基准文件：`E:/zzzz/mini/MiniCode-项目定义-基准.md`（定位/边界/5R/4类任务）。
> 本文件是三项目简历里 MiniCode 的**可贴简历定稿**。改任何内容回本文件，别只看记忆摘要。
> ⚠️ 硬边界：非 Coding Agent、非 RAG、非通用办公平台；实习身份，公司画像【待补充真实实习信息】不编造。

---

## 一、简历可贴版本（步4 修订定稿）

**项目名**：研发项目智能协作助手（简历不出现 MiniCode / Claude Code）

**技术栈**：TypeScript、Node.js、LLM API（Tool Calling）、JSON Schema / Zod、JSONL 事件流

**一句话简介**：面向研发团队的项目资料与文档协作 Agent，利用 LLM Tool Calling 编排文件搜索、读取、汇总与受控写入，支持多步骤任务执行、写前确认与会话恢复。

**项目定位**：实习项目 · 企业研发团队内部 AI 效率工具（公司画像【待补充真实实习信息】）

**Bullets（四段式 · 动词已降级 · 已去内部名 · 已换危险词）**：

**R01** ｜针对项目周报整理、跨文件资料汇总等多步骤文档任务难以通过单次模型调用完成、执行过程缺乏统一终止条件的问题，基于模型-工具-模型循环的 Agent 执行模式，参与实现 Agent 执行循环（协调模型推理、工具调用、结果回填与最大步数终止保护），实现了多步骤文档任务的闭环执行与终止可控。

**R08** ｜针对模型输出到文件工具执行之间缺乏参数契约与校验、错误参数易直接作用于文件系统的问题，基于 JSON Schema 与注册表模式，参与设计并实现统一的工具定义与注册调度契约（为文件列举、搜索、读取、写入与 diff 应用等工具提供参数描述、运行时校验与统一执行入口），实现了工具调用的统一校验与受控调度。

**R09** ｜针对项目资料逐渐增多、跨文件查找时无界读取导致上下文与成本失控的问题，基于受控观察设计，实现了面向项目资料目录的文件工具（目录列举、关键词搜索、分页读取，配合工作目录限制、路径规范化与单次输出规模上限），控制了 Agent 单次读取规模并支持按需续读。

**R10** ｜针对 AI 直接修改团队共用文档存在静默误改风险的问题，参与实现文件写入工具的写前确认机制（修改先生成统一 diff 预览，用户确认后才执行写入，确认动作由用户完成而非模型自行通过），降低了文档误改风险。

**R18** ｜针对文档整理任务可能跨次执行中断、修改过程需要回看依据的问题，参与实现基于 JSONL 事件流的会话记录机制（逐事件记录消息、工具调用与结果，支持从历史事件恢复会话、基于已有事件创建单用户探索分支），实现了任务的可恢复与操作过程的可回溯。

---

## 二、禁区词替换表（面试口径全局生效）

| ❌ 禁用 | ✅ 换成 |
|---|---|
| 审批流 / 写前审查流程 | 写前确认机制 |
| 安全落地 | 统一校验与受控调度 |
| 可审计 | 可回溯 / 保留事件记录 |
| 稳定闭环 | 闭环执行 + 终止保护 |
| 路径安全 | 工作目录限制 + 路径规范化 |
| 内容检索 | 关键词搜索 |

**严禁引出**：RAG / MCP / Skill / Context Compression / 多 Agent / Agent Safety Platform / Evaluation Framework / 权限状态机(PermissionManager=R12) / 沙箱 / 复杂权限生命周期。

---

## 三、三条护身答案（审查暴露的新追问，必背）

1. **「runAgentTurn / ToolDefinition 是你们内部命名吗？」** →「这些是我描述时用的概念化命名，实际以团队代码为准。」（简历正文不出现这些名字，只在口头讲解用）
2. **「JSON Schema 和 Zod 两份定义怎么防漂移？」** →「Schema 给模型生成端、运行时校验管执行端，确实存在两份定义，靠统一的工具定义封装维护一致性；单一事实源自动生成是已知改进方向。」（答不出细节时的诚实降级）
3. **「maxSteps 按什么计数？」** →「按执行步数——每轮模型推理加工具执行计一步，触顶即带诊断终止，防止无限循环。」（千万别现场扩展成超时/重试机制，那是 R02 未选点）

---

## 四、Q&A 全包（步5，共 11 问）

### 【R01】Q01.1 / Q01.2 / Q01.3
- **Q01.1 循环怎么跑**：用户任务进执行循环 → 模型返回文本或工具调用 → 工具经注册表调度执行 → 结果回填消息列表 → 继续推理 → 直到最终结果或最大步数终止。理由：资料动态发现，先定位后读取。局限：单用户单任务，异常恢复在状态机层面。
- **Q01.2 为什么多轮**：单次请求看不到中间结果（纪要内容、搜索命中），每轮带回真实结果让模型逐步补全信息。局限：上下文膨胀，靠 R09 受控读取控制每轮进量。
- **Q01.3 怎么防死循环**：最大步数保护，触顶带诊断终止，不无限空转。
- **弱点追问**：「一次多个工具调用？」→按调用列表逐个调度，结果按序号回填。｜「空响应重试？」→那属异常状态机，我主要做循环编排+终止保护。

### 【R08】Q08.1 / Q08.2
- **Q08.1 为什么 Schema + 运行时校验两层**：Schema 描述参数给模型生成端引导；运行时校验在真正执行前验证实际到达参数，执行端兜底。局限：两份定义，靠统一封装维护一致，自动生成是改进方向。
- **Q08.2 Registry 解决什么**：统一注册/查找/执行/销毁，循环按名调度不 care 细节，新增工具只注册不改循环。局限：Registry 只管调度契约，工具本身自建注册，没做外部 MCP 生态。

### 【R09】Q09.1 / Q09.2
- **Q09.1 为什么三工具**：列举看结构、搜索定位命中、读取取内容，配合让 Agent 自己找到资料。**这不是 RAG**——没做 Embedding/向量库，读了什么全有记录。
- **Q09.2 为什么分页限制输出**：先读关键段按需续读，配合上限+元数据引导，只让必要信息进上下文。
- **弱点追问**：「这不是 RAG 吗？」→不是，RAG 建向量索引语义召回；这是 Agent 主动文件查找+受控读取，更轻可审计。「大规模索引？」→资料到千份级才值得上检索层。
- **降级路径**：「语义搜索更好？」→「大规模+需语义召回才值得，我还需补检索知识。」

### 【R10】Q10.1 / Q10.2
- **Q10.1 写前确认流程**：文件写入工具先算统一 diff → 生成预览 → 用户审阅确认 → 才落盘；确认由用户完成非模型自行通过。理由：不引沙箱不上权限平台，写前确认性价比最高；拒绝则不写，保留 diff 供调整。
- **Q10.2 为什么 diff 优于"确认写入"按钮**：裸确认信息量低，diff 展示增删行+上下文，给足人判断材料。
- **弱点追问**：「新建/删除文件走这流程吗？」→凡文件写入工具产生的修改都生成 diff 预览，删除属高风险同样须确认。｜「权限状态机？」→那属更完整权限系统，我们只做写文件这道确认闸。
- **降级路径**：被问权限生命周期 →「当前只做写文件确认闸，没做成通用权限平台。」

### 【R18】Q18.1 / Q18.2
- **Q18.1 为什么 JSONL**：逐事件追加写入（消息/工具调用/结果/边界标记一行一事件），崩溃不易损坏、天然可回放。局限：单机单用户没上数据库，度量分析是独立层。
- **Q18.2 为什么 fork**：从某点创建独立探索分支，试不同思路不污染原会话；单用户重试，不承担多人协作/文档合并。
- **弱点追问**：「能重建最终文档状态吗？」→事件流记执行过程与结果，文件最终版本以实际写入为准。｜「fork 多人共用？」→不能，是单用户重试分支。
- **降级路径**：被问分支合并/版本控制 →「不在当前范围，fork 只是会话层重试分支。」

---

## 五、典型任务（面试举例用，4 类）

1. **项目周报整理**：搜纪要→读进度→提取完成/延期/下周计划→生成周报（R01 主/R09 辅）
2. **会议纪要整理**：定位纪要→读→提取待办→按负责人/截止时间整理（R01+R09）
3. **项目文档修改**：找文档→读原文→生成修改方案→算 diff→用户确认→写入（**R10 主**/R08 辅）
4. **跨文件信息查找**：如"汇总最近一月支付模块延期问题"→搜索多文件→分批读→综合→返回结论（**R09 主**/R01 辅）

---

## 六、待补充真实实习信息（步6 前必问，不编造）

公司名 / 公司行业 / 规模 / 部门名称 / 团队人数 / IM 类型 / 真实客户数 / 用户数 / QPS / 性能提升数据 / ROI / 节省人力百分比 / 实际协作方式 / 真实职责。
以上若面试被顺"实习"追问，必须从**真实实习**长出来；MiniCode 与那团队无关，临场现编必口径打架。
