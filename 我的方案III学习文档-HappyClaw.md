# 我的 HappyClaw 学习清单（渠道抽象 / 定时调度 / Profile 治理 / 登录认证 / RBAC 分权）

> 我的背景：二本秋招 / 目标岗位 AI 应用开发 / 技术栈 TypeScript（补课中）/ 已学完 MiniCode（执行循环、工具契约）+ Craft（多后端抽象、MCP 接入、多端传输）。HappyClaw 最不一样的：**执行循环交给 Claude Agent SDK 托管**，我做的是循环外围的治理（人设/权限/认证/调度），且首次碰到「多用户产品化」。
>
> ⚠️ 选点 v2（2026-08-24）：**R25（行为评测底座）已移除，改为 R19（认证与会话管理）**；评测思想降为轻量业务 Review + 典型 Case 回归，不单独成点。本清单已同步。
>
> 资料在哪看：**网页端** `hhh/Agent学习资源库/Agent学习资源库-单文件版.html` → 首页「项目 CodeWiki」分类 → **「HappyClaw CodeWiki」（标记 HC，共 30 篇）**。本文档「前置hc/主hc/辅助hc」列写的是 HC 章节号 + 章节名，**前置hc 先读、主hc 是点本身、辅助hc 被追问才展开**，直接按号点。
>
> 配套文件：简历与面试口径看 `E:/zzzz/mini/HappyClaw-定稿.md`（五条 bullet + 14 条防线 + Q&A）；三项目关系看 `E:/zzzz/mini/三项目联动-步6.md`。

---

## 一、简历写什么

| 简历点 | 角色 | 一句话 |
| --- | --- | --- |
| R20 | RBAC 三态 + 工作区所有权 + IM Owner Gate | 权限分级、归属清晰（**主深挖**） |
| R14 | 定时任务调度器 | 物化调度 + 租约互斥（次深挖） |
| R19 | Cookie Session 认证（后台登录） | 认证加固、防爆破（讲清即可，但 be 住「认证 vs 授权」分界） |
| R15 | Agent Profile 四段式 Prompt | 人设治理、确认短语（讲清即可，但定义项目身份） |
| R07 | 七渠道 IM 统一抽象 | 渠道解耦、适配器（讲清即可） |

**方向定位**：权限（R20 主深挖）+ 调度（R14 次深挖）+ 认证（R19，Medium 好防守）+ 人设（R15 身份锚点）+ 渠道（R07 入口）。主深挖 R20、次深挖 R14 的理由见定稿 §6 补课清单（R20 第一、R14 第二——两个超档点零防线雷最多）；R19 是 v2 新增，Scope 清晰（Web 后台登录认证），与 R20 成「认证 vs 授权」安全双支柱。

**⚠️ 与前两个项目的三处根本不同（学之前先认清）**：
1. 执行循环是 Claude Agent SDK 托管的（MiniCode 自写、Craft 后端抽象、本条**连循环都不自己写**）——我的增量在循环外围：能用的工具（TOOLS 段）、谁能动（RBAC）、谁能登录（认证）、何时跑（调度）。
2. 首次碰到「多用户」——但口径是「设计目标」不是规模声明（定稿 §5-3），别讲成「有很多用户在用」。
3. 评测**不单独成点**——只保留轻量业务 Review + 典型 Case 回归两个轻动作（定稿 §5-14），不被深挖评测方法论；前两个项目的评测对比不再主打。

关键技术栈：TypeScript、Node.js、Hono、SQLite、React、Docker、WebSocket、Claude Agent SDK、IMChannel 适配器、租约调度、RBAC 三态、Cookie Session / HMAC 签名认证。

---

## 二、一条线：前置知识 → 点本身，逐点列清

> 每个点按「前置hc → 主hc(点本身) → QA」一条线顺下来。「前置hc」=不先读就讲不清点本身的章节，先读它再进主章节；「辅助hc」=被追问时的扩展弹药，只在被问才展开。前置是面试被追问时的兜底弹药，但**只在被问到才展开**，别主动铺开。

### 单元 0 · 全局骨架（先花半天，所有单元地基）

- **点本身**：三入口（多渠道消息/定时任务/管理后台）→ Agent 层（客服/运营/售后数字员工）→ 业务工具 → 执行结果 → Evaluation 层 → Evaluator 质检闭环。执行循环由 SDK 托管；SQLite 持久化；Docker 部署。
  - 掌握门槛：能画「一条 IM 消息进来 → 数字员工处理 → 结果回渠道」的链路；能说出「谁是我写的、谁是 SDK 给的」分界（这是联动 N2「框架增量」题的地基）。
- QA：无直接题，但 §5-1 世界观头罩（自托管+演示垂直场景）+ 所有四段式都靠这张图撑。
- 定位（地基，无前置）：**HC-01 项目概览** + **HC-09 主服务架构与核心模块划分** +（浏览）**HC-10 Agent Runner 与 IPC 通信协议**

### 单元 1 · R20 RBAC 三态 + 工作区所有权 + IM Owner Gate（主深挖 · 超档点）

- **前置知识**（前置hc：先读 **HC-06 三层模型**、**HC-14 渠道挂载与会话绑定边界**，再进点本身）：
  - 多用户授权：工作区（数据归属单元）与用户/角色的关系 → HC-06（智能体-工作区-会话三层模型，工作区=数据归属单元）；为什么 admin 不该天然 bypass（分权模型 vs 超级管理员）→ 同 HC-24。
  - 发送者身份在 IM 侧的表示：每条消息来自一个渠道账号（发送者 ID），Web 登录身份与 IM 渠道身份是两回事 → HC-14（渠道挂载与会话绑定边界，owner_im_id 写入时机）。
- **点本身**：多角色共用工作区时读写删无分级、admin 权限过大 → canAccessGroup/canModifyGroup/canDeleteGroup 三态分离判定 + admin 不自动 bypass 工作区所有权 + IM Owner Gate（渠道原生发送者 ID 比对 owner_im_id）。
  - 掌握门槛（定稿 §6 列的第一优先源码题）：多用户定位口径、管理后台配受控所有权转移、IM Owner Gate 判定链。
    写全：① admin 管理用户与系统配置、不管工作区数据；② 三态分离（修改前先查 access，delete 是否隐含 modify）；③ IM Owner Gate：消息到达→解析渠道原生发送者 ID→与 owner_im_id 比对→不匹配拒执行且不回执（防探测）→owner_im_id 在绑定渠道账号时写入。
  - 必须先背：定稿 §5-1 头罩 + §5-3 多用户话术 + §5-4 OwnerGate 判定链 + §5-5 孤儿回收。
- QA：Q20.1（interview-qa.md）；定稿 Part1 R20 组三问 + Part3 弱点表 #2/#3/#4
- 定位：前置 **HC-06 + HC-14**；主 **HC-24 RBAC 权限模型与资源隔离**；辅助 **HC-25 密钥加密与凭据管理**（权限与凭据相邻）

### 单元 2 · R14 定时任务调度器（次深挖 · 超档点）

- **前置知识**（前置hc：先读 **HC-10 Agent Runner 与 IPC**、**HC-07 Host 与 Container 双执行模式**，再进点本身）：
  - 执行循环由 SDK 托管，但「什么时候触发执行」是平台自己的事——这是双入口之一（主动触发）→ HC-10（执行侧衔接）。
  - 多实例部署时同一任务会被重复执行 → 需要互斥 → HC-07（isolated 任务独立会话/在容器内执行，隔离语境）。
- **点本身**：重复性动作靠人工按时执行、多实例下重复执行与竞争 → Occurrence Materialization 调度器：先把「该跑的一次」物化为记录再消费执行（解决 backfill+并发）；SQLite 租约互斥（lease_owner/lease_token/lease_expires_at）保证同轮次仅一个实例执行；cron 最小 60s/interval/once 三类型；不可重入。
  - 掌握门槛（定稿 §6 第二优先）：**占锁 HOW 层必背**——占锁=条件更新+租期判断（UPDATE 带 WHERE lease_expires_at 过期条件，受影响行数定赢家，SQLite 单写者保证原子）；lease_token=认领凭据，续租/交接凭它校验归属，防过期实例拿旧 token 污染；心跳续租延长租期防「没跑完被抢」；被追到极端时钟场景退「设计原理清楚，并发用例在测试套件验证过，极端时钟漂移压测没做」。
  - 背定稿 §5-2（多实例口径）+ §5-6（lease HOW）+ §5-7（materialization 动机）。
- QA：Q14.1；定稿 Part1 R14 组四问 + Part3 弱点表 #1/#9
- 定位：前置 **HC-10 + HC-07**；主 **HC-19 定时任务调度与异常恢复**；辅助 **HC-23 数据目录结构与一致性备份恢复**（租约状态落 DB 的可靠性）

### 单元 3 · R19 Cookie Session 认证（讲清即可 · v2 新增 · 安全双支柱之一）

- **前置知识**（前置hc：先读 **HC-09 主服务架构与核心模块划分**、**HC-24 RBAC 权限模型与资源隔离**，再进点本身）：
  - 管理后台是配置渠道、人设、权限的入口——谁登录后台、登录会话安不安全，决定了整个数字员工体系是否被接管 → HC-09（管理后台入口/路由）。
  - 「认证 vs 授权」分界（定稿 §5-11）：R19=Web 后台登录认证（你是谁），R20=授权（你能做什么，含 IM Owner Gate）→ HC-24（授权模型作对照）。**R19 只碰认证，绝不滑向运行时身份/IM 授权**。
- **点本身**：登录 Cookie 可伪造、接口可被爆破 → Cookie Session 认证体系（单条自洽链：服务端生成随机 token 存会话表 → HMAC 签名作防篡改兜底 → 常量时间比较查会话 → __Host- 前缀 Secure Cookie → per-IP + per-username 双层限流）。
  - 掌握门槛：能脱口背出上面那条**自洽链**（这是 D 终审判的最致命雷——别把「HMAC 签名」和「opaque 随机 token」讲成两个并列方案）；能答「为什么随机 id 还要 HMAC」（随机 id 防不可预测+服务端可即时吊销，HMAC 是防篡改兜底）；能答「密钥怎么管」（环境变量注入、不进代码不落盘，轮换=换 key+清会话表）；被追 CSRF/session fixation →「Web 安全第二层是演进方向，个人自托管后台无并发无资金操作所以没上」（定稿 §5-13）。
  - 背定稿 §5-11（认证 vs 授权分界）+ §5-12（token 自洽链 + key 管理）+ §5-13（Web 安全第二层演进方向）。
- QA：Q19.1（常量时间比较）；定稿 Part1 R19 组 + Part3 弱点表 #5/#6/#7
- 定位：前置 **HC-09 + HC-24**；主 **HC-25 密钥加密与凭据管理**（认证/会话/签名）；辅助 **HC-10 Agent Runner 与 IPC 通信协议**（认证与运行时的衔接）；如源码有认证专门模块按 W09 定位。

### 单元 4 · R15 Agent Profile 四段式 Prompt（讲清即可 · 项目身份锚点）

- **前置知识**（前置hc：无强前置；HC-06 本身即顶层身份模型）：
  - Profile 是数字员工的「顶层身份」，工作区是隔离边界、Session 是会话——三层的从属关系 → HC-06。
- **点本身**：人设散装 prompt 缺约束 → 四段式 IDENTITY/SOUL/AGENTS/TOOLS 正交（我是谁/价值观底线/工作规则/工具策略），append/replace 双模式，两阶段 AI 辅助 + 确认短语。
  - 掌握门槛：能答四段各管什么、为什么正交（改规则不动身份底线）；AGENTS 段是声明性工作规则**不是可执行编排引擎**（red line）；确认短语为什么单用户也成立（防对话里改坏人设）。
- QA：Q15.1 + Q15.2；定稿 Part1 R15 组三问
- 定位：主 **HC-06 智能体-工作区-会话三层模型**（Profile 顶层身份）+ **HC-08 Agent 能力治理：Skills、MCP 与 Plugins**（TOOLS 段/能力加载与治理）

### 单元 5 · R07 七渠道 IM 统一抽象（讲清即可）

- **前置知识**（前置hc：先读 **HC-01 项目概览**，理解「多渠道只是统一入口」框架定位）：
  - 各渠道消息格式/附件/会话模型差异；渠道与 Agent 解耦（画像思想③：重点不是 IM 技术，是「多渠道只是统一入口」）→ HC-01。
- **点本身**：逐渠道接 SDK 使 Agent 核心耦合渠道差异 → IMChannel 统一接口（七渠道）适配器模式，收发接口统一。
  - 掌握门槛：能答「各渠道功能完全一致吗」（不一致：微信 iLink 仅 P2P、WhatsApp 有风控，按公共能力设计+适配器层显式暴露差异）；能答「加一个新渠道做什么」（写一个适配器，Agent 核心零改动）。
  - 诚实边界：企微/Slack 走外置桥接非原生。
- QA：Q07.1；定稿 Part1 R07 组三问
- 定位：前置 **HC-01**；主 **HC-13 IM 渠道架构与多账号连接池**；辅助 **HC-15 飞书会话语义与原生话题映射** + **HC-16 渠道可靠性状态机**（可作扩展讲但别深入）

---

## 三、资料速查

**路线 A · 网页端（日常学习，推荐）**：打开 HTML → 「项目 CodeWiki」→ HappyClaw CodeWiki（HC）。章节对照：

| 单元 | 前置hc（先读） | 主hc | 辅助hc |
| --- | --- | --- | --- |
| 单元 0 | —（地基） | HC-01 / HC-09 / HC-10（项目概览 / 主服务架构 / Agent Runner 与 IPC） | — |
| 单元 1（R20 主深挖） | HC-06 / HC-14（三层模型 / 渠道挂载与会话绑定） | HC-24（RBAC 权限模型与资源隔离） | HC-25（密钥加密与凭据管理） |
| 单元 2（R14 次深挖） | HC-10 / HC-07（Agent Runner 与 IPC / 双执行模式） | HC-19（定时任务调度与异常恢复） | HC-23（数据目录结构与备份恢复） |
| 单元 3（R19 认证） | HC-09 / HC-24（主服务架构 / RBAC 授权模型） | HC-25（密钥加密与凭据管理） | HC-10（Agent Runner 与 IPC） |
| 单元 4（R15） | —（无强前置） | HC-06 / HC-08（三层模型 / Agent 能力治理） | — |
| 单元 5（R07） | HC-01（项目概览框架） | HC-13（IM 渠道架构与多账号连接池） | HC-15 / HC-16（飞书话题映射 / 渠道可靠性） |

**路线 B · 深挖源码（带读时用）**：源码在 `E:/zzzz/mini/happyclaw付费版内参/happyclaw-main/`（container/agent-runner 用 Claude Agent SDK；src/ 下有各 IM 渠道/调度/RBAC/认证模块）。带读优先级（=定稿 §6）：① RBAC 三态函数 + IM Owner Gate deny 分支 + owner_im_id 写入时机（HC-24/14）② 租约占锁 SQL 原句 + 心跳续租 + materialization 记录生命周期（HC-19）③ **Cookie session token 生成 + HMAC 签名 + 常量时间比较 + 双层限流**（HC-25，v2 新增）④ Profile 四段存储形态 + 确认短语生成校验（HC-06/08）⑤ 任一渠道适配器读一条收发链路（HC-13）。

---

## 四、注意事项

### 四条红线（违反即穿帮）

1. **论文数字只进口头，绝不进 bullet**：MT-Bench 85% vs 81%、position/verbosity/consistency 三偏置、τ-bench pass^8 <25%、AI Control 15%→92%、26.1% 技能漏洞率——只在被问评测/安全/治理时作前沿认知。评测点已移除，这些数字更少有机会用到。
2. **未实现的不说成已做**：LLM 裁判/pass^k 行为评测（只有方法论认知，且评测点已移除则避谈）、Evaluator Agent 自动评审→修改闭环、技能自动进化（R26 未选）、运行时多租户硬隔离、RAG/知识库、Web 安全第二层（SameSite/CSRF token/登录重生成 session）。
3. **不编造量化指标**：验证效果只用定性表述（分级管控落地/归属清晰/安全调度/认证加固）。
4. **多用户是设计目标，不是规模声明**：绝不暗示「有企业在用/服务了几个租户」；被问就答「自托管形态、设计目标、验证到工作区/角色级，硬隔离是演化方向」。

### 降级预案（讲不稳就换）

| 原要点 | 降级到 |
| --- | --- |
| R20（主深挖） | 退「我实现的是授权矩阵层（访问/修改/删除判定），Web 认证与运行时隔离是别的模块」；被追 OwnerGate 细节 → 泛化为「渠道身份匹配」授权判断层 |
| R14（次深挖） | 退「设计原理层」：互斥靠 DB 条件更新非应用锁，宁可错过不重复；再退 → 转 R15/R20 工程层 |
| R19（认证） | 退「取舍层」：随机 token 存会话表+服务端可即时吊销，HMAC 只是防篡改兜底；再退 → 转 R20 授权矩阵；被追 CSRF/fixation → 「Web 安全第二层是演进方向」 |
| R15 | 退回只说「四段正交 + 确认短语」概念，不深挖 replace 不可清除实现 |
| R07 | 退回「至少解决单渠道耦合」的适配器价值，不深入可靠性状态机 |

### 其他

- **主次分明**：R20 能扛三层源码追问（决定「多用户」定位成立与否），R14 扛 HOW 层，R19 背熟自洽链+key 管理+Web 安全第二层边界（D 判的雷，必须背），R15 定义项目身份、R07 讲清流程。
- 每个超档点（R20/R14）学完必须能**对着定稿 §6 的源码题**自答一遍，答不出的回炉；R19 补充课对应定稿 §6 第 3 项。
- 只学这 6 个单元，不贪多；每点过 30 秒电梯测试（对着定稿 talking points 说）。
- 与前两个项目的口径切分（步6 联动已备，**评测部分 v2 需调整**）：工具处理演进线「契约（MiniCode）→ 接入（Craft）→ 治理（本条 TOOLS 段）」；**评测演进线已断（本条 R25 移除）**——不再主打「工程验证→行为评测」，改为**每个机制自带验证**（R14 并发用例/R15 replace 不可清除/R20 越权用例）+ 可信赖=安全双支柱（R19+R20）+ 工具调用留痕结论可追溯（定稿 §5-14 质量保障叙事）；状态记录「可恢复（MiniCode JSONL）→ 可隔离（Craft Session）」保留，R25 的「可归因」不再作为本条卖点。
- 回答一律四段式：解决问题 → 实现方式 → 设计取舍 → 局限与扩展。
